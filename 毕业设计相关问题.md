# 基于大模型的个性化食谱生成系统

## 毕业设计（论文）相关问题与解答

---

## 一、项目概述类问题

### 1.1 请简单介绍一下你的项目

**答：**
本项目是一个基于大模型的个性化食谱生成系统（Recipe Generator System）。系统针对用户现有的食材库存，通过调用大语言模型（LLM）技术，为用户生成符合其口味偏好、饮食限制和健康目标的个性化食谱方案。

系统采用前后端分离架构：
- **前端**：基于 Vue.js 3 + Element Plus 构建，提供用户友好的交互界面
- **后端**：基于 Spring Boot 3.2 + MyBatis-Plus 构建，提供 RESTful API
- **AI模块**：集成阿里云百炼平台（MiniMax-M2.7 模型）实现文本理解和图像生成

**创新点：**
1. 多模态食材识别：支持文本和图片两种方式输入食材
2. AI驱动的食谱生成：根据用户偏好定制化生成食谱
3. 智能营养分析：基于中国居民膳食指南进行营养评估
4. 本地图片生成：使用 MiniMax image-01 模型生成与食谱匹配的美食图片

---

### 1.2 项目的背景和意义是什么？

**答：**

**背景：**
1. 现代人工作繁忙，缺乏饮食规划，常常导致食材浪费
2. 人们对健康饮食的需求日益增长，需要个性化的营养方案
3. 大语言模型技术的发展，为智能食谱推荐提供了技术可能
4. 传统食谱推荐系统缺乏个性化和智能化

**意义：**
1. **实用价值**：帮助用户合理利用现有食材，减少浪费
2. **健康价值**：根据用户的饮食限制和健康目标，提供科学的营养建议
3. **技术价值**：探索大模型在垂直领域的应用实践
4. **经济价值**：降低烹饪门槛，提升生活品质

---

### 1.3 项目的技术选型依据是什么？

**答：**

| 技术 | 选型 | 依据 |
|------|------|------|
| 前端框架 | Vue.js 3 | 轻量级、组件化生态完善、学习曲线平缓 |
| UI组件库 | Element Plus | Vue3生态成熟、文档完善、中文友好 |
| 后端框架 | Spring Boot 3.2 | 生态成熟、自动配置简化开发 |
| ORM框架 | MyBatis-Plus | 国产优秀、简化CRUD、内置分页插件 |
| 数据库 | MySQL 8.0 | 关系型数据库成熟稳定、兼容性良好 |
| 缓存 | Redis | 高性能、支持多种数据结构 |
| 认证 | JWT | 无状态、可扩展、支持跨域 |
| AI模型 | MiniMax-M2.7 | 中文理解能力强、支持多模态 |

---

## 二、需求分析类问题

### 2.1 系统有哪些功能模块？

**答：**

```
系统功能模块
├── 1. 用户管理模块
│   ├── 用户注册/登录
│   ├── 用户信息管理
│   └── 饮食偏好设置（口味、健康目标、饮食限制）
├── 2. 食材管理模块
│   ├── 文本输入食材
│   ├── 图片识别食材（AI图像识别）
│   ├── 食材列表查看/编辑/删除
│   └── 食材库存管理
├── 3. 食谱生成模块
│   ├── 选择食材生成食谱
│   ├── AI生成个性化食谱（考虑口味偏好、饮食限制）
│   ├── 食谱详情查看（含AI生成配图）
│   └── 烹饪步骤展示
├── 4. 收藏管理模块
│   ├── 个人收藏食谱
│   ├── 取消收藏
│   └── 查看收藏列表
├── 5. 营养分析模块
│   ├── 食谱营养成分计算
│   ├── 营养评估与建议
│   └── 健康目标匹配度分析
└── 6. 智能图片模块
    ├── MiniMax image-01 生成食谱配图
    ├── 本地图片保存与复用
    └── 图片搜索（备用：Spoonacular API）
```

---

### 2.2 系统的非功能需求有哪些？

**答：**

**性能需求：**
- 食谱生成响应时间 < 10秒（AI调用）
- 页面加载时间 < 3秒
- 数据库查询响应 < 500ms

**安全需求：**
- 用户密码加密存储（BCrypt）
- JWT Token认证
- 敏感接口鉴权
- SQL注入防护

**可用性需求：**
- 系统可用性 > 99.9%
- 错误提示友好化
- 操作反馈及时

**可扩展性需求：**
- 模块化设计
- 配置与代码分离
- 预留AI模型扩展接口

**兼容性需求：**
- 主流浏览器兼容（Chrome、Firefox、Edge）
- 移动端适配

---

### 2.3 系统的用例图和流程图

**答：**

**主要用例：**
1. 用户注册 → 设置饮食偏好
2. 用户登录 → 管理食材
3. 添加食材（手动输入/图片识别）
4. 生成食谱（选择食材 → AI生成）
5. 查看食谱详情（文字+配图）
6. 收藏/取消收藏食谱
7. 营养分析
8. 查看我的食谱/收藏

**核心业务流程：**
```
用户登录
    ↓
添加食材（文本/图片）
    ↓
选择食材生成食谱
    ↓
AI考虑：口味偏好 + 饮食限制 + 健康目标
    ↓
生成食谱方案 + 生成配图
    ↓
查看详情/收藏/营养分析
    ↓
完成
```

---

## 三、数据库设计类问题

### 3.1 请介绍一下数据库设计

**答：**

**数据库共6张表：**

| 表名 | 说明 | 主要字段 |
|------|------|---------|
| user | 用户表 | id, username, password, age, gender, phone, role, create_time |
| user_preference | 用户偏好表 | id, user_id, health_goal, diet_limit, taste_preference, cooking_level, max_cooking_time |
| food_material | 食材表 | id, name, type, quantity, shelf_life, nutrition_id, user_id, create_time |
| recipe | 食谱表 | id, name, english_name, image_keyword, image_url, cooking_time, difficulty_level, steps, food_ids, collect_count, ingredients, tools, tips, suitable_crowd, cuisine_style, flavor_profile, servings, nutrition_brief |
| user_collection | 收藏表 | id, user_id, recipe_id, create_time |
| nutrition | 营养表 | id, food_id, calorie, protein, fat, carbohydrate, vitamin, mineral |

**ER图关系：**
- User 1:1 UserPreference
- User 1:N FoodMaterial
- User 1:N Recipe
- User 1:N UserCollection N:1 Recipe（多对多中间表）
- FoodMaterial N:1 Nutrition

---

### 3.2 为什么这样设计数据库？

**答：**

1. **用户表与偏好表分离**：用户基本信息和个性化偏好分离存储，便于独立管理和扩展
2. **食材与营养分离**：营养数据可复用，减少数据冗余
3. **收藏表独立**：支持多用户收藏同一食谱，统计收藏次数
4. **食谱存储food_ids字符串**：食材关系以字符串存储，简化查询
5. **使用逻辑删除考量**：当前采用物理删除，未来可扩展为逻辑删除

---

## 四、关键技术类问题

### 4.1 如何使用大模型生成食谱？

**答：**

**Prompt工程设计：**
```
系统提示：你是一个专业厨师和食谱设计师。根据用户提供的食材，生成个性化食谱。

返回格式：JSON数组，每个食谱包含：
- name: 中文食谱名称
- englishName: 英文名称（用于图片搜索）
- imageKeyword: 图片搜索关键词（Spoonacular优化）
- cookingTime: 烹饪时间（分钟）
- difficultyLevel: 难度等级
- steps: 详细烹饪步骤
- foodIds: 使用食材列表

考虑因素：
- 用户健康目标（减脂/控糖等）
- 饮食限制/过敏原
- 口味偏好（辣/淡等）
- 烹饪水平
```

**AI调用流程：**
1. 接收用户选择的食材ID列表
2. 查询食材名称
3. 构建Prompt（包含用户偏好信息）
4. 调用MiniMax-M2.7 API
5. 解析JSON返回结果
6. 返回食谱列表

---

### 4.2 如何实现图片识别食材？

**答：**

**实现方案：**
主路径：阿里云百炼 **Qwen-VL Plus**（模型标识 `qwen3-vl-plus`，OpenAI 兼容模式），在 `AiApiClient.callQwenVlImageUnderstand` 中实现。
兜底路径：**MiniMax vision**（`callMiniMaxImageUnderstand`），以及 `McpImageClient` 通过 Python 脚本调用 Claude CLI 的 MiniMax MCP `understand_image` 工具（仅当 Qwen-VL/MiniMax 均不可用时）。

**调用方式（Qwen-VL Plus）：**
```java
POST https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
{
  "model": "qwen3-vl-plus",
  "messages": [
    {"role": "user", "content": [
      {"type": "image_url", "image_url": {"url": "data:image/jpeg;base64,..."}},
      {"type": "text", "text": "请识别图片中的食材，给出 name/type/quantity(g)/shelfLife(天)"}
    ]}
  ]
}
```

**调用分支（`FoodServiceImpl.recognizeFood` → `AiApiClient.callImageUnderstandApi`）：**
1. 如果配置了 `ai.dashscope-api-key`，走 Qwen-VL Plus；
2. 否则走 MiniMax 多模态；
3. 两者均可降级至 `McpImageClient.recognizeImageViaMcp`。

所有路径最终通过 `extractJson` + `parseFoodListFromJson` 解析成 `List<FoodMaterial>` 并入库（绑定 `user_id`）。

---

### 4.3 如何生成与食谱匹配的AI图片？

**答：**

**使用MiniMax image-01模型：**

```java
// 调用MiniMax图片生成API
POST https://api.minimaxi.com/v1/image_generation

{
  "model": "image-01",
  "prompt": "A professional food photography of Kung Pao Chicken, Chinese dish, appetizing presentation, restaurant style, top-down angle, natural lighting, 4K quality",
  "aspect_ratio": "1:1",
  "response_format": "url",
  "n": 1,
  "prompt_optimizer": true
}
```

**本地保存策略：**
1. 生成图片后，下载到本地 `C:/develop/codeBase/bsfood1/picture/`
2. 文件命名：`{食谱名称}.jpg`
3. 相同名称的食谱直接复用本地图片，避免重复生成
4. 通过Spring MVC静态资源映射提供访问

---

### 4.4 如何处理用户饮食限制与食谱生成的冲突？

**答：**

**实现机制：**

1. **用户偏好存储**：
   - diet_limit: 饮食限制（如：海鲜、花生、芒果等过敏原）
   - health_goal: 健康目标（减脂/增肌/控糖/普通）
   - taste_preference: 口味偏好（辣/淡/甜等）

2. **生成前检查**：
   ```java
   // 检查食材是否与饮食限制冲突
   const conflicts = selectedFoods.filter(food =>
     dietLimits.some(limit => food.name.includes(limit))
   );

   if (conflicts.length > 0) {
     // 弹出确认对话框
     // 用户确认后仍可生成
   }
   ```

3. **AI Prompt融入**：
   ```
   饮食限制/过敏原：{dietLimit}
   生成的食谱绝对不能包含这些食材或与其相关的食材
   ```

4. **冲突解决策略**：
   - 提示用户食材冲突
   - 用户可选择忽略警告继续生成
   - 或更换食材重新生成

---

## 五、系统架构类问题

### 5.1 系统的整体架构是什么？

**答：**

**采用四层B/S架构：**

```
┌─────────────────────────────────────────────────┐
│              Presentation Layer                  │
│           Vue.js 3 + Element Plus               │
│    (用户界面 / HTTP请求 / 路由跳转)              │
└─────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────┐
│             Business Logic Layer                │
│         Spring Boot 3.2 Controller              │
│    (业务处理 / 参数校验 / 响应封装)              │
└─────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────┐
│               Data Access Layer                 │
│        MyBatis-Plus + Redis Cache              │
│      (数据持久化 / 缓存 / SQL操作)              │
└─────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────┐
│              Data Storage Layer                 │
│           MySQL 8.0 + Redis                    │
│        (关系数据 / 会话缓存)                    │
└─────────────────────────────────────────────────┘

外部服务：
├── MiniMax API (文本理解 + 图片生成)
├── Spoonacular API (图片搜索备选)
└── 阿里云百炼平台
```

---

### 5.2 为什么采用前后端分离架构？

**答：**

1. **开发效率**：前后端可并行开发
2. **专业分工**：前端专注UI，后端专注业务
3. **技术灵活**：前后端可选不同技术栈
4. **性能优化**：前端可做构建优化、懒加载
5. **接口复用**：同一后端可支持多端（Web/App/小程序）
6. **独立部署**：前后端可独立部署和扩展

---

### 5.3 如何保证系统的安全性？

**答：**

| 安全措施 | 实现方式 |
|----------|----------|
| 密码加密 | BCryptPasswordEncoder |
| 认证机制 | JWT Token（无状态认证） |
| 接口鉴权 | JwtAuthFilter拦截器 |
| SQL注入防护 | MyBatis参数化查询 |
| XSS防护 | 前端输入转义 |
| CSRF防护 | Spring Security CSRF禁用（API模式） |
| CORS配置 | CorsConfig允许跨域 |
| 敏感操作日志 | 后端日志记录 |

---

## 六、开发过程类问题

### 6.1 开发过程中遇到的主要问题及解决方案

**答：**

| 问题 | 解决方案 |
|------|----------|
| AI返回JSON格式错误 | 增强解析逻辑，添加try-catch和修复方法 |
| 数据库表结构与实体不匹配 | 使用ALTER TABLE添加缺失字段 |
| 食谱图片不匹配 | 多级兜底搜索策略 + MiniMax AI生成 |
| 图片生成后不显示 | 配置Spring Security放行 + Vite代理 |
| 中文菜名无法搜索图片 | AI返回englishName和imageKeyword |
| 网络超时 | 合理设置timeout + 重试机制 |
| 图片重复生成 | 本地缓存，相同名称直接复用 |

---

### 6.2 项目亮点和创新点

**答：**

1. **多模态食材输入**：支持文本+图片两种方式识别食材
2. **AI个性化生成**：考虑用户口味偏好、饮食限制、健康目标
3. **智能图片匹配**：
   - MiniMax image-01 生成与食谱名匹配的美食图片
   - 本地保存复用，避免重复生成
4. **营养科学分析**：基于《中国居民膳食指南》进行评估
5. **用户画像管理**：完善的饮食偏好设置和食材管理
6. **实时冲突检测**：食材选择时检测与饮食限制的冲突

---

### 6.3 测试策略

**答：**

**单元测试：**
- Service层业务逻辑测试
- 工具类方法测试

**集成测试：**
- API接口测试（使用curl/Postman）
- 数据库读写测试

**系统测试：**
- 前后端联调测试
- 完整业务流程测试

**测试用例示例：**
1. 用户注册 → 登录流程
2. 添加食材 → 生成食谱 → 查看详情
3. 食材冲突检测提示
4. 营养分析功能
5. 收藏/取消收藏

---

## 七、答辩准备类问题

### 7.1 如果时间不够，你会简化哪些功能？

**答：**
优先级排序：

1. **核心功能保留**：
   - 用户登录注册
   - 食材管理（文本输入）
   - 食谱生成（AI）
   - 食谱查看

2. **可简化功能**：
   - 图片识别食材（保留文本输入）
   - 营养分析（保留基础展示）
   - 本地图片生成（使用Spoonacular搜索替代）

3. **可延后功能**：
   - 高级营养图表
   - 食材替换建议
   - 社交分享功能

---

### 7.2 系统有哪些不足和未来改进方向？

**答：**

**当前不足：**
1. AI生成结果不稳定，偶尔JSON解析失败
2. 图片生成依赖网络，存在延迟
3. 未实现完整的管理员功能
4. 缺乏离线支持（PWA）
5. 移动端适配不够完善

**未来改进：**
1. **功能扩展**：
   - 批量导入食材（扫码/拍照）
   - 食材保质期提醒
   - 一周食谱规划
   - 购物清单自动生成

2. **技术优化**：
   - 引入消息队列，优化AI调用
   - 图片CDN加速
   - 本地缓存优化

3. **智能化提升**：
   - 机器学习优化推荐算法
   - 用户行为分析
   - 社区互动功能

---

### 7.3 毕业设计的工作量统计

**答：**

| 模块 | 代码量 | 说明 |
|------|--------|------|
| 后端Controller | ~800行 | 7个控制器，约30个接口 |
| 后端Service | ~1200行 | 业务逻辑处理 |
| 实体类 | ~600行 | 6个实体 |
| 工具类 | ~500行 | AI客户端、图片下载器 |
| 前端页面 | ~2000行 | 6个主要页面 |
| 数据库 | 6张表 | 含索引、约束 |
| 文档 | ~50页 | 设计文档、API文档 |

**总代码量**：约5000+ 行

---

## 八、技术细节追问

### 8.1 JWT Token是如何工作的？

**答：**

```
1. 用户登录 → 后端验证密码 → 生成Token返回
2. Token包含：用户ID、过期时间、签名
3. 前端存储在localStorage
4. 后续请求携带：Authorization: Bearer <token>
5. 后端JwtAuthFilter拦截验证
6. 验证通过设置SecurityContext
7. 控制器获取当前用户：SecurityContextHolder.getContext().getAuthentication()
```

---

### 8.2 如何解决AI返回JSON解析失败的问题？

**答：**

**多重防护机制：**

```java
// 1. 首先尝试直接解析
try {
    JSONArray arr = JSON.parseArray(json);
    // 解析食谱列表
} catch (Exception e) {
    // 2. 提取JSON部分重新解析
    String extracted = extractJson(aiResponse);
    try {
        JSONArray arr = JSON.parseArray(extracted);
        // 再次解析
    } catch (Exception ex) {
        // 3. 修复不完整JSON
        String fixed = fixMalformedJson(json);
        JSONArray arr = JSON.parseArray(fixed);
    }
}

// 修复方法示例
private String fixMalformedJson(String json) {
    // 找到第一个[
    // 找到最后一个]
    // 截取中间部分
    // 补全可能缺失的引号等
}
```

---

### 8.3 为什么使用Redis？它在系统中起什么作用？

**答：**

**当前用途：**
- 用户会话缓存（如果需要）
- 热点数据缓存

**可扩展用途：**
- AI调用结果缓存
- 食谱搜索结果缓存
- 分布式Session存储
- 消息队列（延迟任务）

**未来集成：**
```yaml
spring:
  redis:
    host: localhost
    port: 6379
    password: 123456
    database: 0
```

---

### 8.4 图片生成和下载的流程是怎样的？

**答：**

```
1. 前端：用户点击食谱详情
   ↓
2. 后端：检查本地是否存在同名图片
   ↓ (存在)
3. 直接返回本地路径 → 显示图片
   ↓ (不存在)
4. 调用MiniMax image-01 API生成图片
   ↓
5. 下载AI返回的图片URL到本地
   ↓
6. 保存到 C:/develop/codeBase/bsfood1/picture/{食谱名}.jpg
   ↓
7. 返回访问路径给前端
   ↓
8. 前端通过 /picture/{食谱名}.jpg 访问显示
```

---

## 九、其他常见问题

### 9.1 系统的并发能力如何？

**答：**
- Tomcat默认线程池：200线程
- MySQL连接池：HikariCP默认最大10连接
- 预估并发：100-200用户同时在线
- AI调用为瓶颈（外部API限制）

### 9.2 管理员功能目前实现到什么程度？

**答：**
管理员模块已具备独立的 `AdminController` + `AdminService` + `AdminServiceImpl`：
- 用户列表查询 / 删除：`GET /api/admin/user/list`、`DELETE /api/admin/user/delete`
- 全量食材列表 / 删除：`GET /api/admin/food/list`、`DELETE /api/admin/food/delete`
- 全量食谱列表 / 删除：`GET /api/admin/recipe/list`、`DELETE /api/admin/recipe/delete`
- 前端：`Admin.vue` + `router.js` 中的 `beforeEach` 守卫 `user.role === 'ADMIN'`

**尚未到位**：后端侧未对 `/api/admin/**` 做基于 `role` 的二次鉴权（目前依赖前端路由守卫），生产化前应在 `JwtAuthFilter` 中把 `role` 写入 `GrantedAuthority`，`SecurityConfig` 里用 `.requestMatchers("/api/admin/**").hasRole("ADMIN")`。

### 9.3 数据备份策略是什么？

**答：**
- 数据库：每日增量备份 + 每周全量备份
- 本地图片：随代码仓库Git管理（或独立备份）
- 配置文件：不提交到Git（敏感信息）

### 9.4 系统部署架构是怎样的？

**答：**
```
开发环境：Windows + MySQL + Redis（本地）
生产环境（建议）：
├── 前端：Nginx + Vue静态资源
├── 后端：Spring Boot JAR (Linux服务器)
├── 数据库：MySQL 8.0 (独立服务器)
└── 缓存：Redis (可选)
```

---

## 十、架构与调用链速查

需要"某个前端动作具体落到哪几个类哪几个方法"的时候，请查阅仓库根目录下 **《系统架构与功能调用链文档.md》**。它按模块列出了：

- 前端视图文件（如 `Food.vue`）
- 前端 API 函数（如 `foodApi.recognize`）
- HTTP 路径（如 `POST /api/food/recognize`）
- Controller 方法（如 `FoodController.recognize`）
- Service 实现（如 `FoodServiceImpl.recognizeFood`）
- 底层工具或外部 API（如 `AiApiClient.callQwenVlImageUnderstand` / `McpImageClient`）
- 最终的数据存储目标（表、Redis key、`picture/` 目录）

该文档与本问答集对齐，可直接作为答辩时回答"你这个功能从前端到后端都走了哪些类"的依据。

---

## 附录：项目技术栈总结

| 层次 | 技术 | 版本 |
|------|------|------|
| 前端框架 | Vue.js | 3.x |
| UI组件库 | Element Plus | 最新 |
| 构建工具 | Vite | 4.x |
| 后端框架 | Spring Boot | 3.2.0 |
| ORM框架 | MyBatis-Plus | 3.5.x |
| 数据库 | MySQL | 8.0 |
| 缓存 | Redis | 6.x / 7.x |
| 安全框架 | Spring Security | 6.x |
| AI模型 | MiniMax-M2.7 | - |
| 图片生成 | MiniMax image-01 | - |
| JDK | Java | 17+ |

---

**文档说明**：本文档基于《基于大模型的个性化食谱生成系统》项目整理，涵盖了软件工程专业毕业设计常见问题及参考回答。可根据实际情况进行调整和补充。
