# 基于大模型的个性化食谱生成系统

## Design and Implementation of a Personalized Recipe Generation System Based on Large Language Model

---

## 摘要

随着人们生活节奏的加快和健康意识的提升,如何合理利用现有食材、规划健康饮食成为现代人面临的重要问题。传统的食谱推荐系统存在个性化程度低、食材匹配不准确、无法考虑用户饮食限制等缺点。本文设计并实现了一套基于大模型的个性化食谱生成系统。

本系统采用前后端分离架构,前端基于Vue.js 3和Element Plus构建用户界面,后端基于Spring Boot 3.2和MyBatis-Plus实现业务逻辑,数据库采用MySQL 8.0进行数据存储、Redis缓存热点数据。系统集成了阿里云百炼平台的Qwen-VL Plus(`qwen3-vl-plus`)模型实现食材图像识别,调用MiniMax-M2.7大语言模型实现智能食谱生成和营养分析,使用MiniMax image-01模型生成与食谱匹配的美食图片并在本地落盘复用。

系统主要功能包括:用户管理、食材管理(支持文本输入和图片识别)、个性化食谱生成、营养分析、收藏管理和AI图片生成。系统在生成食谱时会综合考虑用户的口味偏好、饮食限制和健康目标,实现真正的个性化推荐。生成的食谱图片通过AI模型实时生成并本地缓存,既保证了图片与食谱的匹配度,又提高了图片加载效率。

经单元测试和集成测试,系统达到了预期的功能目标,为用户提供了一个便捷、高效、个性化的智能食谱管理平台。

**关键词**: 个性化食谱生成,大语言模型,Spring Boot,Vue.js,人工智能

---

## Abstract

With the acceleration of modern life pace and the improvement of health consciousness, how to make rational use of existing ingredients and plan a healthy diet has become an important issue for modern people. Traditional recipe recommendation systems suffer from low personalization, inaccurate ingredient matching, and inability to consider users' dietary restrictions. This paper designs and implements a personalized recipe generation system based on large language model.

The system adopts a front-end and back-end separation architecture. The front-end is built with Vue.js 3 and Element Plus, the back-end is implemented with Spring Boot 3.2 and MyBatis-Plus, and MySQL 8.0 is used for data storage. The system integrates MiniMax-M2.7 large language model from Alibaba Cloud Bailian platform for intelligent recipe generation and ingredient recognition, uses MiniMax image-01 model to generate food images matching recipes, and uses Spoonacular API as a backup for image search.

The main functions of the system include: user management, ingredient management (supporting text input and image recognition), personalized recipe generation, nutrition analysis, collection management, and AI image generation. When generating recipes, the system comprehensively considers users' taste preferences, dietary restrictions, and health goals to achieve truly personalized recommendations. Generated recipe images are cached locally after AI generation, which ensures the matching degree between images and recipes while improving image loading efficiency.

Through unit testing and integration testing, the system has achieved the expected functional goals and provides users with a convenient, efficient, and personalized intelligent recipe management platform.

**Keywords**: Personalized Recipe Generation, Large Language Model, Spring Boot, Vue.js, Artificial Intelligence

---

## 目录

摘要 I
Abstract II

第1章 绪论 1
1.1 课题背景 1
1.2 研究意义 2
1.3 国内外研究现状分析 3
1.4 论文的主要内容 4

第2章 系统需求分析与概要设计 6
2.1 系统需求分析 6
2.1.1 系统功能概述 6
2.1.2 系统详细功能需求 9
2.1.3 系统运行流程 14
2.1.4 系统非功能需求 17
2.2 系统结构设计 18
2.2.1 系统物理结构设计 18
2.2.2 系统逻辑结构设计 19
2.3 系统时序图 20
2.3.1 用户注册登录时序图 20
2.3.2 食材管理时序图 21
2.3.3 食谱生成时序图 22
2.3.4 图片生成时序图 23

第3章 系统详细设计 24
3.1 系统功能模块接口设计 24
3.1.1 用户管理模块接口设计 24
3.1.2 食材管理模块接口设计 25
3.1.3 食谱管理模块接口设计 26
3.1.4 收藏管理模块接口设计 27
3.1.5 营养分析模块接口设计 28
3.1.6 管理员模块接口设计 28
3.2 数据库设计 29
3.2.1 数据库概念模型设计 29
3.2.2 数据库物理模型设计 35

第4章 系统实现与测试 40
4.1 核心功能实现 40
4.1.1 AI食谱生成实现 40
4.1.2 图片识别实现(Qwen-VL Plus 主路径 + MiniMax/Claude MCP 兜底) 44
4.1.3 AI图片生成实现 46
4.2 系统实现效果 48
4.3 系统测试 55
4.3.1 单元测试 55
4.3.2 集成测试 60

结论 61
致谢 62
参考文献 63

> **配套工程文档**：为方便答辩时定位"某功能从前端到后端究竟调用了哪些类与方法",本论文附带仓库根目录下的《系统架构与功能调用链文档.md》,其中按模块给出了"View → 前端 API → Controller → Service → Mapper/AI客户端 → 数据存储"的精确调用链路,以及更新后的整体架构图。

---

## 第1章 绪论

### 1.1 课题背景

随着我国经济的快速发展和人民生活水平的不断提高,人们的饮食习惯发生了巨大变化。一方面,现代人工作繁忙,生活节奏加快,往往没有足够的时间和精力去规划每日的饮食,导致食材浪费现象严重。据相关统计,我国每个家庭平均每周因食材保存不当或规划不合理而浪费的食物价值可达数十元。另一方面,人们对健康饮食的需求日益增长,减脂、控糖、养生等健康目标成为越来越多人的追求。

传统的食谱推荐系统主要采用基于规则或协同过滤的方法,存在以下主要问题:

(1) **个性化程度低**: 传统系统通常只根据食材种类进行简单匹配,无法充分考虑用户的个体差异,如口味偏好、健康目标、饮食限制等因素。

(2) **食材识别困难**: 用户往往不知道自己现有食材的具体种类和数量,传统系统要求用户手动输入,操作繁琐且容易出错。

(3) **图片匹配度差**: 多数系统使用的食物图片来自公开图库,与实际生成的食谱名称往往不匹配,影响用户体验。

(4) **营养分析缺失**: 缺乏科学的营养成分分析和评估,无法为用户提供符合其健康目标的食谱建议。

近年来,大语言模型(Large Language Model, LLM)技术的快速发展为上述问题的解决提供了新的可能。以MiniMax、GPT-4、阿里云通义千问等为代表的大语言模型展现了强大的自然语言理解和生成能力,能够理解用户的模糊表述,并生成符合上下文的高质量内容。同时,多模态大模型的发展使得图像理解和图像生成成为现实,这为智能食谱系统的构建提供了坚实的技术基础。

本课题正是基于这一技术背景下提出的,旨在利用大语言模型和多模态模型技术,设计并实现一个智能化的个性化食谱生成系统,为用户提供便捷、高效、科学的饮食管理方案。

### 1.2 研究意义

本课题的研究具有重要的理论意义和实际应用价值:

**(1) 推动大语言模型在垂直领域的应用研究**

本课题探索了大语言模型在饮食管理这一垂直领域的应用方法和最佳实践。通过设计合适的提示词工程(Prompt Engineering),使大语言模型能够准确理解用户的饮食需求,并生成符合要求的食谱方案。同时,研究了如何利用多模态模型实现食材的图像识别和美食图片的自动生成,为大语言模型在其他垂直领域的应用提供了参考。

**(2) 提升用户饮食管理的效率和科学性**

通过本系统,用户可以快速了解现有食材情况,并在AI的帮助下生成符合个人口味和健康目标的食谱方案。相比传统的手动规划方式,大大提高了饮食管理的效率。同时,系统提供的营养分析功能,能够帮助用户科学地评估和调整饮食结构,养成健康的饮食习惯。

**(3) 减少食材浪费,促进资源节约**

研究表明,家庭食材浪费的主要原因是没有合理的食材规划。本系统通过帮助用户合理规划食材使用,结合食材保质期管理等功能,能够有效减少因食材过期或保存不当而造成的浪费,为建设资源节约型社会贡献力量。

**(4) 具有良好的商业应用前景**

随着人们健康意识的提升和"互联网+餐饮"的发展,智能食谱管理系统具有广阔的市场前景。本课题的研究成果可以为相关企业提供技术储备和产品化参考,具有重要的商业价值。

### 1.3 国内外研究现状分析

#### 1.3.1 国内研究现状分析

国内在智能食谱系统方面的研究起步较晚,但近年来发展迅速。一些互联网公司和科研机构相继推出了各自的食谱推荐平台。

**大型互联网公司的布局**: 各大外卖平台和美食APP如美团、饿了么等纷纷加入智能食谱推荐功能,通过分析用户的点餐历史和偏好,为其推荐相似菜品。部分健康管理APP如薄荷健康等也推出了食谱管理功能,侧重于营养分析和健康饮食指导。

**科研机构的研究**: 国内多所高校和研究机构开展了食谱推荐系统的研究,主要采用协同过滤、内容推荐和知识图谱等技术。部分研究尝试引入深度学习方法来提高推荐的准确性。

**存在的问题**: 国内的食谱系统普遍存在与本课题所述类似的问题,如个性化程度不高、食材识别困难、缺乏智能图片匹配等。特别是在利用大语言模型进行食谱生成方面,目前还处于探索阶段,缺乏成熟的应用方案。

#### 1.3.2 国外研究现状分析

国外的智能食谱系统研究相对成熟,一些代表性成果值得关注:

**美国的研究**: 美国麻省理工学院等机构开展了一系列关于智能厨房和饮食自动化的研究。IBM开发的"Chef Watson"系统利用认知计算技术为用户生成创意食谱,能够根据用户口味和现有食材提供个性化建议。Google的"Food Pairing"项目则通过分析食材的化学成分发现食材搭配规律。

**欧洲的研究**: 欧洲一些国家的研究侧重于健康饮食和营养分析。欧盟资助的"IFIC"项目研究如何通过信息技术改善人们的饮食习惯。部分研究将食谱推荐与慢性病管理相结合,为特定人群提供饮食指导。

**日本的研究**: 日本在食材识别和智能厨房方面有较多研究。部分研究利用图像识别技术自动识别食材种类和新鲜度,为食谱推荐提供依据。日本企业开发的智能冰箱能够自动记录食材存量并提醒用户及时食用。

**技术趋势**: 国外的研究呈现出几个明显趋势:一是多模态技术的融合,将图像、文本、语音等多种信息形式综合处理;二是知识图谱的深入应用,将食材营养知识、烹饪技法等结构化存储;三是个性化和情境感知能力的提升,能够考虑更多用户特征和环境因素。

### 1.4 论文的主要内容

本论文主要研究如何利用大语言模型技术设计并实现一个个性化的食谱生成系统。论文的主要工作包括以下几个方面:

**(1) 需求分析与系统设计**

通过对市场调研和用户访谈,深入分析了个性化食谱生成系统的功能需求和非功能需求。确定了系统的主要功能模块,包括用户管理、食材管理、食谱生成、营养分析、收藏管理等。采用前后端分离的B/S架构,设计了系统的整体结构和技术方案。

**(2) 数据库设计**

根据系统需求,设计了包含用户表、用户偏好表、食材表、食谱表、收藏表、营养表等6张表的数据库结构。采用MySQL作为数据存储引擎,利用MyBatis-Plus简化数据访问层的开发。设计了清晰的表结构和索引,保证了数据的完整性和查询效率。

**(3) 后端系统实现**

基于Spring Boot 3.2框架,实现了系统的后端服务。主要包括:用户认证模块(基于JWT的无状态认证)、食材管理模块(支持文本输入和图片识别)、食谱生成模块(调用大语言模型API)、图片生成模块(调用MiniMax image-01模型)、营养分析模块等。设计了统一的RESTful API接口,便于前端调用和系统集成。

**(4) 前端系统实现**

基于Vue.js 3和Element Plus组件库,实现了用户界面。主要包括:用户登录注册页面、食材管理页面(支持图片上传和AI识别)、食谱生成页面、食谱详情页面、营养分析页面、个人收藏页面等。采用了响应式设计,支持多种屏幕尺寸设备的访问。

**(5) AI功能集成**

系统集成了多家AI能力：调用阿里云百炼平台的Qwen-VL Plus(`qwen3-vl-plus`)完成食材图像识别(主路径),调用MiniMax-M2.7大语言模型完成文本理解、食谱生成与营养分析,调用MiniMax image-01模型生成美食图片。设计了多级兜底的识别与生成策略(Qwen-VL → MiniMax vision → Claude CLI MCP；AI图片生成 → 本地缓存复用)。

**(6) 系统测试**

采用单元测试和集成测试相结合的方式,对系统的各个功能模块进行了全面测试。设计了详细的测试用例,覆盖了正常流程和异常流程。测试结果表明,系统达到了预期的功能目标和性能要求。

---

## 第2章 系统需求分析与概要设计

### 2.1 系统需求分析

#### 2.1.1 系统功能概述

根据前期的市场调研和用户需求分析,本系统旨在为用户提供一个便捷、高效、个性化的智能食谱管理平台。系统主要面向有以下需求的用户:

(1) 希望合理规划日常饮食,避免食材浪费的家庭用户;
(2) 有特定健康目标(如减脂、控糖、增强体质等)的用户;
(3) 对饮食营养有较高要求,注重科学膳食的用户;
(4) 希望探索新菜式,丰富家庭饮食种类的用户。

基于上述需求分析,本系统设计了以下主要功能模块:

**1. 用户管理模块**

用户管理模块是系统的基础模块,负责用户的注册、登录和个人信息管理。主要功能包括:

- **用户注册**: 用户可以通过用户名、密码进行注册,系统验证用户信息的合法性和唯一性。
- **用户登录**: 支持用户名密码登录,返回JWT Token用于身份认证。
- **个人信息管理**: 用户可以修改个人信息,包括年龄、性别、电话等。
- **饮食偏好设置**: 用户可以设置自己的口味偏好(辣/淡/甜等)、健康目标(减脂/增肌/控糖/普通)、饮食限制(海鲜过敏/素食主义等)、烹饪水平(新手/进阶/熟练)等。

**2. 食材管理模块**

食材管理模块是系统的核心模块之一,负责用户食材的添加、编辑和删除操作。主要功能包括:

- **文本输入食材**: 用户可以通过文本方式输入食材名称和数量,系统自动识别食材类型。
- **图片识别食材**: 用户可以上传食材图片,系统通过AI图像识别技术自动识别食材种类,减少用户手动输入的工作量。
- **食材列表查看**: 用户可以查看当前库存的所有食材,包括食材名称、类型、数量、保质期等信息。
- **食材编辑/删除**: 用户可以修改食材信息或删除不需要的食材。

**3. 食谱生成模块**

食谱生成模块是系统的核心功能,负责根据用户选择的食材和偏好设置生成个性化食谱。主要功能包括:

- **选择食材生成**: 用户从库存食材中选择若干种,系统根据所选食材生成相应的食谱方案。
- **AI智能生成**: 调用大语言模型API,综合考虑用户的口味偏好、饮食限制、健康目标等因素,生成符合用户需求的食谱。
- **生成数量设置**: 用户可以设置期望生成的食谱数量(1-5个),系统按要求生成相应数量的候选食谱。
- **食谱详情查看**: 用户可以查看食谱的详细信息,包括名称、烹饪时间、难度等级、详细步骤等。
- **AI配图展示**: 系统为每个食谱生成匹配的美食图片,提升视觉效果和用户参考价值。

**4. 收藏管理模块**

收藏管理模块允许用户收藏自己喜欢的食谱,方便日后查阅。主要功能包括:

- **收藏食谱**: 用户可以收藏喜欢的食谱到个人收藏夹。
- **取消收藏**: 用户可以取消对某个食谱的收藏。
- **查看收藏列表**: 用户可以查看自己的所有收藏食谱。

**5. 营养分析模块**

营养分析模块为用户提供食谱的营养成分分析和健康评估。主要功能包括:

- **营养成分计算**: 根据食谱使用的食材和烹饪方法,计算热量、蛋白质、脂肪、碳水化合物等主要营养成分。
- **营养评估**: 参考《中国居民膳食指南》对食谱进行营养评估,指出营养是否均衡。
- **改善建议**: 根据用户的健康目标,给出针对性的营养改善建议。

**6. 智能图片模块**

智能图片模块负责为食谱生成匹配的美食图片。主要功能包括:

- **AI图片生成**: 调用MiniMax image-01模型,根据食谱名称生成专业的美食图片。
- **本地图片保存**: 将生成的图片保存到本地目录,便于快速加载和重复使用。
- **图片复用机制**: 相同名称的食谱直接使用已有图片,避免重复调用API,节省资源和成本。

**7. 系统管理模块**

系统管理模块面向管理员角色(`user.role = 'ADMIN'`),提供系统数据的集中维护能力。主要功能包括:

- **用户列表与删除**: `AdminController` 暴露 `/api/admin/user/list` 与 `/api/admin/user/delete`。
- **食材列表与删除**: `/api/admin/food/list` / `/api/admin/food/delete`。
- **食谱列表与删除**: `/api/admin/recipe/list` / `/api/admin/recipe/delete`。
- **前端访问控制**: `router.js` 的 `beforeEach` 守卫对 `/admin` 路径校验 `user.role === 'ADMIN'`。

系统的功能结构图如图2-1所示:

```
┌─────────────────────────────────────────────────────────────────┐
│                      个性化食谱生成系统                              │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ 用户管理  │  │ 食材管理  │  │ 食谱生成  │  │ 收藏管理  │       │
│  │  模块    │  │  模块    │  │  模块    │  │  模块    │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
│  ┌──────────┐  ┌──────────┐                                      │
│  │ 营养分析  │  │智能图片  │                                      │
│  │  模块    │  │  模块    │                                      │
│  └──────────┘  └──────────┘                                      │
└─────────────────────────────────────────────────────────────────┘
```

图2-1 系统功能结构图

#### 2.1.2 系统详细功能需求

**2.1.2.1 用户信息管理**

用户信息管理功能包括用户注册、登录、个人信息修改和饮食偏好设置。

用户信息管理功能的用例图如图2-2所示:

```
┌─────────────────────────────────────┐
│           用户                         │
├─────────────────────────────────────┤
│ · 注册账号                           │
│ · 登录系统                           │
│ · 修改个人信息                        │
│ · 设置饮食偏好                        │
│ · 设置口味偏好                        │
│ · 设置健康目标                        │
│ · 设置饮食限制                        │
│ · 设置烹饪水平                        │
└─────────────────────────────────────┘
```

图2-2 用户信息管理用例图

主要业务流程如下:

(1) **注册流程**: 用户输入用户名、密码、确认密码等信息,系统验证用户名唯一性,密码加密后存储到数据库,注册成功返回登录界面。

(2) **登录流程**: 用户输入用户名和密码,系统验证用户身份,验证通过后生成JWT Token返回给客户端,客户端后续请求携带Token进行身份认证。

(3) **饮食偏好设置流程**: 用户登录后可以设置或修改个人饮食偏好,包括口味偏好(辣/淡/甜/酸等)、健康目标(减脂/增肌/控糖/普通)、饮食限制(海鲜过敏/花生过敏/素食主义/清真等)、烹饪水平(新手/进阶/熟练),系统将偏好信息与用户账户关联存储。

**2.1.2.2 食材管理**

食材管理功能负责用户食材的添加、查看、编辑和删除操作。

食材管理功能的用例图如图2-3所示:

```
┌─────────────────────────────────────┐
│           用户                         │
├─────────────────────────────────────┤
│ · 添加食材(文本输入)                   │
│ · 添加食材(图片识别)                   │
│ · 查看食材列表                        │
│ · 编辑食材信息                        │
│ · 删除食材                           │
└─────────────────────────────────────┘
```

图2-3 食材管理用例图

主要业务流程如下:

(1) **文本输入食材**: 用户输入食材名称和数量(g为单位),系统自动识别食材类型(蔬菜/肉类/水产/豆制品等),保存到数据库。如果该食材已有营养数据,自动关联营养信息。

(2) **图片识别食材**: 用户上传食材图片,系统以阿里云百炼 **Qwen-VL Plus**(`qwen3-vl-plus`)作为主要识别通道,MiniMax 多模态模型作为备选,并保留通过 Claude CLI 调用 MiniMax MCP 的 `understand_image` 能力作为最终兜底(由 `McpImageClient` 实现)。AI 返回识别出的食材名称列表,用户确认后系统自动创建相应的食材记录。

(3) **查看食材列表**: 系统显示用户的所有食材,按类型分组展示,显示食材名称、数量、保质期(如果有)等核心信息。

(4) **编辑/删除食材**: 用户可以修改食材的名称、数量、保质期等信息,或删除不需要的食材。

**2.1.2.3 食谱生成**

食谱生成是系统的核心功能,根据用户选择的食材和偏好设置生成个性化食谱。

食谱生成功能的用例图如图2-4所示:

```
┌─────────────────────────────────────┐
│           用户                         │
├─────────────────────────────────────┤
│ · 选择食材                           │
│ · 设置生成数量                        │
│ · 生成食谱                           │
│ · 查看食谱详情                        │
│ · 收藏食谱                           │
│ · 营养分析                           │
└─────────────────────────────────────┘
```

图2-4 食谱生成用例图

主要业务流程如下:

(1) **选择食材**: 用户从食材列表中选择若干种食材,可以选择1种或多种食材的组合。

(2) **设置参数**: 用户设置期望生成的食谱数量(1-5个),系统考虑用户的饮食偏好设置。

(3) **生成食谱**: 系统调用大语言模型API,传递用户选择的食材信息和偏好设置,AI综合分析后返回JSON格式的食谱列表,包括中文名称、英文名称(用于图片搜索)、烹饪时间、难度等级、详细步骤等。

(4) **生成配图**: 系统调用MiniMax image-01模型,根据食谱名称生成匹配的美食图片,并下载保存到本地目录。

(5) **查看详情**: 用户可以点击查看任意食谱的详细信息,包括完整的烹饪步骤和高清美食图片。

(6) **收藏/营养分析**: 用户可以对感兴趣的食谱进行收藏或进入营养分析页面查看详细的营养成分。

**2.1.2.4 收藏管理**

收藏管理功能允许用户收藏和查看自己感兴趣的食谱。

收藏管理功能的用例图如图2-5所示:

```
┌─────────────────────────────────────┐
│           用户                         │
├─────────────────────────────────────┤
│ · 收藏食谱                           │
│ · 取消收藏                          │
│ · 查看收藏列表                       │
└─────────────────────────────────────┘
```

图2-5 收藏管理用例图

主要业务流程如下:

(1) **收藏食谱**: 用户点击食谱的收藏按钮,系统将用户ID和食谱ID关联存储到收藏表。

(2) **取消收藏**: 用户可以在收藏列表中取消对某个食谱的收藏,系统删除相应的收藏记录。

(3) **查看收藏列表**: 用户进入个人收藏页面,系统展示该用户收藏的所有食谱信息。

**2.1.2.5 营养分析**

营养分析功能为用户提供食谱的营养成分计算和健康评估。

营养分析功能的用例图如图2-6所示:

```
┌─────────────────────────────────────┐
│           用户                         │
├─────────────────────────────────────┤
│ · 选择食谱进行营养分析                  │
│ · 查看营养成分                        │
│ · 查看营养评估                        │
│ · 查看改善建议                        │
└─────────────────────────────────────┘
```

图2-6 营养分析用例图

主要业务流程如下:

(1) **选择食谱**: 用户从食谱列表或收藏列表中选择一个食谱,点击"营养分析"按钮。

(2) **计算营养成分**: 系统根据食谱使用的食材种类和数量,查询各食材的营养数据,计算总热量、蛋白质、脂肪、碳水化合物等主要营养成分。

(3) **生成评估和建议**: 系统参考《中国居民膳食指南》的推荐标准,对食谱的营养结构进行评估,指出是否适合用户的健康目标,并给出改善建议。

**2.1.2.6 智能图片**

智能图片功能为食谱生成匹配的美食图片,提升用户体验。

智能图片功能的用例图如图2-7所示:

```
┌─────────────────────────────────────┐
│           系统                         │
├─────────────────────────────────────┤
│ · 生成AI图片                         │
│ · 保存图片到本地                      │
│ · 提供图片访问                       │
│ · 图片复用检查                       │
└─────────────────────────────────────┘
```

图2-7 智能图片用例图

主要业务流程如下:

(1) **图片生成请求**: 当用户查看食谱详情时,系统检查本地是否已有该食谱名称的图片。

(2) **本地图片检查**: 如果本地已有同名图片,直接使用本地图片;如果不存在,调用MiniMax image-01 API生成新图片。

(3) **图片下载保存**: AI返回图片URL后,系统下载图片并保存到本地指定目录,文件命名为食谱名称。

(4) **图片访问**: 通过配置的静态资源路径,前端可以访问到生成的美食图片进行展示。

#### 2.1.3 系统运行流程

系统的核心运行流程包括用户注册登录流程、食材管理流程、食谱生成流程和图片生成流程。

**(1) 用户注册登录流程**

用户注册登录流程如图2-8所示:

```
用户                     系统                      数据库
 │                       │                        │
 │────注册请求───────────>│                        │
 │   (用户名、密码)       │                        │
 │                       │────验证唯一性──────────>│
 │                       │<───返回验证结果────────│
 │                       │                        │
 │<──返回注册结果────────│                        │
 │                       │                        │
 │                       │                        │
 │                       │                        │
 │────登录请求───────────>│                        │
 │   (用户名、密码)       │                        │
 │                       │────查询用户信息────────>│
 │                       │<───返回用户信息─────────│
 │                       │                        │
 │                       │────验证密码────────────>│
 │                       │<───验证结果────────────│
 │                       │                        │
 │                       │────生成JWT────────────>│
 │                       │                        │
 │<──返回Token──────────│                        │
 │                       │                        │
```

图2-8 用户注册登录流程图

**(2) 食材管理流程**

食材管理流程如图2-9所示:

```
用户                     系统                      AI服务
 │                       │                        │
 │────上传食材图片───────>│                        │
 │                       │────调用图像识别───────>│
 │                       │<───返回食材列表────────│
 │                       │                        │
 │<──显示识别结果────────│                        │
 │                       │                        │
 │────确认添加───────────>│                        │
 │                       │────保存食材信息───────>│
 │                       │                        │
 │<──返回添加成功────────│                        │
```

图2-9 食材管理流程图

**(3) 食谱生成流程**

食谱生成流程是系统的核心流程,涉及用户偏好检查、AI食谱生成、AI图片生成等多个环节,如图2-10所示:

```
用户                     系统                    AI服务              数据库
 │                       │                      │                    │
 │────选择食材───────────>│                      │                    │
 │                       │                      │                    │
 │                       │────获取用户偏好──────>│                    │
 │                       │<───返回偏好信息───────│                    │
 │                       │                      │                    │
 │                       │────调用食谱生成API───>│                    │
 │                       │<───返回食谱JSON───────│                    │
 │                       │                      │                    │
 │                       │────解析并保存食谱────────────────────────>│
 │                       │                      │                    │
 │<──显示食谱列表────────│                      │                    │
 │                       │                      │                    │
 │────查看详情───────────>│                      │                    │
 │                       │                      │                    │
 │                       │────检查本地图片────────────────────────>│
 │                       │                      │                    │
 │                       │[图片不存在]           │                    │
 │                       │────调用图片生成API───>│                    │
 │                       │<───返回图片URL────────│                    │
 │                       │                      │                    │
 │                       │────下载保存图片────────────────────────>│
 │                       │                      │                    │
 │<──显示详情和图片──────│                      │                    │
```

图2-10 食谱生成流程图

**(4) 图片生成流程**

图片生成流程如图2-11所示:

```
食谱生成请求
      │
      ▼
┌─────────────┐
│检查本地是否   │
│已有同名图片   │
└─────────────┘
      │
      ├───存在──→ 返回本地图片路径
      │
      └───不存在──→ 调用MiniMax image-01 API
                              │
                              ▼
                    ┌─────────────────┐
                    │ 生成美食图片URL  │
                    └─────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │ 下载图片到本地    │
                    │ C:/picture/     │
                    └─────────────────┘
                              │
                              ▼
                    返回本地访问路径
```

图2-11 图片生成流程图

#### 2.1.4 系统非功能需求

除了功能需求外,系统还需要满足以下非功能需求:

**2.1.4.1 系统性能需求**

(1) **响应时间**: 系统页面加载时间不超过3秒;食谱生成由于涉及AI调用,响应时间不超过10秒。

(2) **并发能力**: 系统能同时支持50个以上用户在线操作;数据库连接池最大连接数不低于10个。

(3) **资源利用率**: 服务器CPU平均利用率不超过70%;内存使用合理,无内存泄漏。

**2.1.4.2 系统安全性需求**

(1) **密码安全**: 用户密码采用BCrypt加密存储,即使数据库泄露也无法直接获取用户密码明文。

(2) **认证机制**: 采用JWT Token进行身份认证,Token设置合理有效期,过期后需要重新登录。

(3) **接口安全**: 所有API接口都需要进行身份验证,防止未授权访问。

(4) **数据安全**: SQL查询使用参数化查询,防止SQL注入攻击。

**2.1.4.3 系统易用性需求**

(1) **界面友好**: 采用清晰直观的界面设计,重要功能入口明显,操作流程简洁。

(2) **易于学习**: 系统操作符合用户习惯,提供必要的操作提示和帮助信息。

(3) **容错性好**: 对用户输入进行校验,给出清晰的错误提示,引导用户正确操作。

**2.1.4.4 系统可维护性需求**

(1) **模块化设计**: 系统采用模块化设计,各功能模块之间松耦合,便于维护和扩展。

(2) **代码规范**: 遵循统一的编码规范,代码结构清晰,注释完善。

(3) **日志完善**: 系统运行关键环节记录日志,便于问题排查和系统监控。

**2.1.4.5 系统可扩展性需求**

(1) **配置分离**: 敏感配置信息外置,支持不修改代码即可更新配置。

(2) **AI服务可替换**: AI服务调用封装在独立模块,便于替换为其他AI服务提供商。

(3) **功能扩展预留**: 系统架构考虑未来可能的功能扩展,如食材扫码添加、食谱分享等。

### 2.2 系统结构设计

#### 2.2.1 系统物理结构设计

系统采用前后端分离的B/S架构,物理结构包括客户端、服务器和外部服务三个层次。

**(1) 客户端层**

客户端为浏览器,用户通过浏览器访问系统。系统支持主流浏览器,包括Google Chrome、Mozilla Firefox、Microsoft Edge等。客户端负责页面渲染和用户交互,不直接与数据库交互,而是通过RESTful API与服务端进行数据交换。

**(2) 应用服务器层**

应用服务器托管前端资源和后端服务:

- **前端资源**: Vue.js 3构建的单页应用(SPA),通过Nginx或开发服务器提供静态资源服务。
- **后端服务**: Spring Boot 3.2应用,处理业务逻辑和数据访问。
- **本地图片服务**: 图片文件存储在服务器本地目录,通过Spring MVC静态资源映射提供访问。

**(3) 数据存储层**

数据存储层包括关系数据库和文件系统:

- **MySQL 8.0**: 存储用户信息、食谱数据、食材数据等结构化数据。
- **本地文件系统**: 存储AI生成的美食图片文件。

**(4) 外部服务层**

系统调用以下外部AI服务:

- **MiniMax API**: 提供大语言模型(MiniMax-M2.7)用于食谱生成和食材识别。
- **MiniMax Image API**: 提供图像生成模型(image-01)用于生成美食图片。
- **Spoonacular API**: 作为图片搜索的备用方案,当AI图片生成失败时提供备选图片。

系统的物理结构如图2-12所示:

```
┌─────────────────────────────────────────────────────────────────┐
│                         客户端 (Browser)                        │
│                 Vue.js 3 + Element Plus SPA                      │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  │ HTTP/HTTPS
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                      应用服务器 (8081)                           │
│  ┌──────────────────┐    ┌──────────────────────────────────┐  │
│  │   Nginx/Vite     │    │      Spring Boot 3.2             │  │
│  │   前端资源服务    │    │  ┌────────┐  ┌────────┐        │  │
│  │                  │    │  │Controller│  │ Service │        │  │
│  │                  │    │  └────────┘  └────────┘        │  │
│  │                  │    │  ┌────────┐  ┌────────┐        │  │
│  │                  │    │  │  Mapper │  │   AI   │        │  │
│  │                  │    │  │         │  │ Client │        │  │
│  │                  │    │  └────────┘  └────────┘        │  │
│  └──────────────────┘    └──────────────────────────────────┘  │
│                              │                    │               │
│                              ▼                    ▼               │
│                    ┌─────────────────┐  ┌─────────────────┐    │
│                    │   MySQL 8.0      │  │  本地文件系统     │    │
│                    │   数据库存储      │  │  /picture/      │    │
│                    └─────────────────┘  └─────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  │ HTTPS
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                       外部AI服务                                  │
│  ┌──────────────────┐    ┌──────────────────────────────────┐  │
│  │   MiniMax API    │    │       Spoonacular API            │  │
│  │  (食谱生成/识别)  │    │        (图片搜索备选)           │  │
│  └──────────────────┘    └──────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

图2-12 系统物理结构图

#### 2.2.2 系统逻辑结构设计

系统采用经典的分层架构,从上到下依次为表示层、业务逻辑层、数据访问层和数据存储层。

**(1) 表示层(Vue.js 3)**

表示层负责用户界面的展示和交互处理。采用Vue.js 3框架,主要组件包括:

- **页面组件**: Home.vue、Food.vue、Recipe.vue、Nutrition.vue、User.vue等
- **API调用**: 通过Axios调用后端RESTful接口
- **状态管理**: 组件内部状态管理

**(2) 业务逻辑层(Spring Boot)**

业务逻辑层负责处理系统的核心业务规则,采用Spring Boot实现,主要模块包括:

- **Controller**: 接收HTTP请求,调用Service处理业务逻辑,返回响应结果
- **Service**: 实现核心业务逻辑,如食谱生成逻辑、食材识别逻辑等
- **AI Client**: 封装对外部AI服务的调用,如AiApiClient

**(3) 数据访问层(MyBatis-Plus)**

数据访问层负责与数据库的交互,采用MyBatis-Plus实现:

- **Mapper**: 定义数据库操作接口,继承BaseMapper
- **实体类**: 对应数据库表结构的Java对象,如User、Recipe、FoodMaterial等

**(4) 数据存储层(MySQL 8.0)**

数据存储层负责数据的持久化存储,采用MySQL 8.0数据库,包含以下主要表:

- **user**: 用户基本信息表
- **user_preference**: 用户偏好表
- **food_material**: 食材表
- **recipe**: 食谱表
- **user_collection**: 收藏表
- **nutrition**: 营养数据表

系统的逻辑结构如图2-13所示:

```
┌─────────────────────────────────────────────────────────────────┐
│                        表示层 (Vue.js 3)                         │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐          │
│  │ Home.vue │  │Food.vue │  │Recipe.vue│  │User.vue │          │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘          │
│       └────────────┴────────────┴────────────┘                  │
│                          │ Axios API调用                        │
└──────────────────────────┼────────────────────────────────────┘
                           │
┌──────────────────────────┼────────────────────────────────────┐
│                   业务逻辑层 (Spring Boot)                       │
│                          │                                      │
│       ┌─────────────────┼─────────────────┐                    │
│       │                 │                  │                    │
│  ┌────┴────┐      ┌────┴────┐       ┌────┴────┐                │
│  │ User    │      │ Recipe  │       │  Food   │                │
│  │Controller│      │Controller│      │Controller│               │
│  └────┬────┘      └────┬────┘       └────┬────┘                │
│       │                 │                  │                    │
│  ┌────┴────┐      ┌────┴────┐       ┌────┴────┐                │
│  │User     │      │Recipe   │       │ Food    │                │
│  │Service  │      │Service  │       │ Service │                │
│  └────┬────┘      └────┬────┘       └────┬────┘                │
│       │                 │                  │                    │
│       └─────────────────┴──────────────────┘                    │
│                          │                                      │
│                    ┌─────┴─────┐                               │
│                    │ AiApiClient│ (调用外部AI服务)              │
│                    └───────────┘                                │
└──────────────────────────┼────────────────────────────────────┘
                           │
┌──────────────────────────┼────────────────────────────────────┐
│               数据访问层 (MyBatis-Plus)                         │
│                          │                                      │
│       ┌─────────────────┼─────────────────┐                    │
│       │                 │                  │                    │
│  ┌────┴────┐      ┌────┴────┐       ┌────┴────┐                │
│  │User     │      │Recipe   │       │ Food    │                │
│  │Mapper   │      │Mapper   │       │ Mapper  │                │
│  └─────────┘      └─────────┘       └─────────┘                │
└──────────────────────────┼────────────────────────────────────┘
                           │
┌──────────────────────────┼────────────────────────────────────┐
│                      数据存储层 (MySQL 8.0)                     │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐            │
│  │  user   │  │recipe   │  │  food   │  │nutrition │            │
│  │  表     │  │  表     │  │  表     │  │   表    │            │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘            │
└─────────────────────────────────────────────────────────────────┘
```

图2-13 系统逻辑结构图

### 2.3 系统时序图

#### 2.3.1 用户注册登录时序图

用户注册登录是系统的基础功能,涉及用户、控制器、服务层和数据访问层的交互,时序图如图2-14所示:

```
用户        前端(Vue)      UserController    UserService     UserMapper    数据库
  │             │              │                │              │           │
  │──注册请求──>│              │                │              │           │
  │             │──POST注册───>│                │              │           │
  │             │              │────检查唯一性───>│              │           │
  │             │              │<───返回结果─────│              │           │
  │             │              │                │              │           │
  │             │              │────保存用户────>│──INSERT─────>│           │
  │             │              │<───返回成功────<──返回结果─────│           │
  │             │<───返回结果──│                │              │           │
  │<──提示成功──│              │                │              │           │
  │             │              │                │              │           │
  │──登录请求──>│              │                │              │           │
  │             │──POST登录───>│                │              │           │
  │             │              │────验证用户────────────────────────>│     │
  │             │              │<───返回用户─────────────────────────│     │
  │             │              │                │              │           │
  │             │              │────密码验证─────│              │           │
  │             │              │<───验证结果─────│              │           │
  │             │              │                │              │           │
  │             │              │────生成JWT────────────────────────>│     │
  │             │              │<───返回Token────────────────────────│     │
  │             │<──返回Token──│                │              │           │
  │<───登录成功──│              │                │              │           │
```

图2-14 用户注册登录时序图

#### 2.3.2 食材管理时序图

食材管理包括文本添加和图片识别两种方式,时序图如图2-15所示:

```
用户        前端(Vue)      FoodController    FoodService     AiApiClient   数据库
  │             │              │                │              │           │
  │──上传图片──>│              │                │              │           │
  │             │──调用AI识别──>│                │              │           │
  │             │              │────图像识别────────────────────────>│      │
  │             │              │<───返回食材列表────────────────────────│      │
  │             │<──显示结果──│                │              │           │
  │             │              │                │              │           │
  │──确认添加──>│              │                │              │           │
  │             │──POST添加───>│                │              │           │
  │             │              │────保存食材────>│──INSERT─────>│          │
  │             │              │<───返回成功────<──返回结果───────│          │
  │             │<──返回成功──│                │              │           │
  │<──添加成功──│              │                │              │           │
```

图2-15 食材管理时序图

#### 2.3.3 食谱生成时序图

食谱生成是系统的核心功能,时序图如图2-16所示:

```
用户        前端(Vue)     RecipeController  RecipeService   AiApiClient   数据库
  │             │              │                │              │           │
  │──选择食材──>│              │                │              │           │
  │──生成食谱──>│              │                │              │           │
  │             │──POST生成───>│                │              │           │
  │             │              │──获取用户偏好────────────────────────>│     │
  │             │              │<──返回偏好─────────────────────────────│     │
  │             │              │                │              │           │
  │             │              │────获取食材信息───────────────────────>│     │
  │             │              │<──返回食材列表──────────────────────────│     │
  │             │              │                │              │           │
  │             │              │                │──调用LLM───>│           │
  │             │              │                │<──返回JSON───│           │
  │             │              │                │              │           │
  │             │              │────保存食谱───────────────────────────>│     │
  │             │              │<───返回成功────────────────────────────│     │
  │             │<──返回食谱列表│                │              │           │
  │<──显示食谱──│              │                │              │           │
```

图2-16 食谱生成时序图

#### 2.3.4 图片生成时序图

图片生成时序图如图2-17所示:

```
RecipeController   ImageDownloader   AiApiClient   MiniMax API    文件系统
      │                  │              │              │            │
      │──生成图片请求───>│              │              │            │
      │                  │              │              │            │
      │                  │<──检查本地图片是否存在─────────────────────│
      │                  │              │              │            │
      │[图片不存在]      │              │              │            │
      │                  │──调用生成API───────────────────────────>│
      │                  │              │              │            │
      │                  │              │<──返回图片URL──────────────│
      │                  │              │              │            │
      │                  │──下载图片──────────────────────────────>│
      │                  │              │              │            │
      │                  │<──保存成功──────────────────────────────│
      │                  │              │              │            │
      │<──返回图片路径──│              │              │            │
```

图2-17 图片生成时序图

---

## 第3章 系统详细设计

### 3.1 系统功能模块接口设计

#### 3.1.1 用户管理模块接口设计

用户管理模块提供用户注册、登录、信息修改和偏好设置等功能。

**主要接口设计:**

| 接口名称 | 请求方式 | 传入参数 | 传出参数 | 功能说明 |
|---------|---------|---------|---------|---------|
| /api/user/register | POST | username, password | code, message, data | 用户注册 |
| /api/user/login | POST | username, password | code, message, data(token) | 用户登录 |
| /api/user/me | GET | (header中携带token) | code, message, data(userInfo) | 获取当前用户信息 |
| /api/user/update | PUT | user对象 | code, message | 更新用户信息 |
| /api/user/preference/get | GET | userId | code, message, data(preference) | 获取用户偏好 |
| /api/user/preference/set | POST | userId, preference对象 | code, message | 设置用户偏好 |

**登录接口详细设计表:**

| 字段 | 类型 | 说明 |
|------|------|------|
| 接口名 | Login | 登录接口 |
| 请求URL | /api/user/login | POST |
| 传入参数 | username(String), password(String) | 用户名和密码 |
| 传出参数 | code(int), message(String), data{token} | 状态码、消息、Token |
| 返回code | 200成功, 400失败 | 登录结果状态码 |

**用户偏好设置接口详细设计表:**

| 字段 | 类型 | 说明 |
|------|------|------|
| 接口名 | setPreference | 设置用户偏好 |
| 请求URL | /api/user/preference/set | POST |
| 传入参数 | userId(Long), healthGoal(String), dietLimit(String), tastePreference(String), cookingLevel(String) | 用户ID及各项偏好设置 |
| 传出参数 | code(int), message(String) | 状态码、消息 |
| 返回code | 200成功, 400失败 | 设置结果状态码 |

#### 3.1.2 食材管理模块接口设计

食材管理模块提供食材的增删改查和图片识别功能。

**主要接口设计:**

| 接口名称 | 请求方式 | 传入参数 | 传出参数 | 功能说明 |
|---------|---------|---------|---------|---------|
| /api/food/add | POST | food对象 | code, message | 添加食材 |
| /api/food/list | GET | userId | code, message, data(list) | 获取食材列表 |
| /api/food/update | PUT | food对象 | code, message | 更新食材 |
| /api/food/delete | DELETE | id | code, message | 删除食材 |
| /api/food/recognizeImage | POST | image(base64) | code, message, data(list) | AI图片识别食材 |

**食材添加接口详细设计表:**

| 字段 | 类型 | 说明 |
|------|------|------|
| 接口名 | addFood | 添加食材 |
| 请求URL | /api/food/add | POST |
| 传入参数 | name(String), type(String), quantity(Double), shelfLife(Integer), userId(Long) | 食材名称、类型、数量、保质期、用户ID |
| 传出参数 | code(int), message(String), data(foodId) | 状态码、消息、新食材ID |
| 返回code | 200成功, 400失败 | 添加结果状态码 |

**图片识别接口详细设计表:**

| 字段 | 类型 | 说明 |
|------|------|------|
| 接口名 | recognizeImageFood | 图片识别食材 |
| 请求URL | /api/food/recognizeImage | POST |
| 传入参数 | imageData(String, base64编码) | 图片Base64数据 |
| 传入参数 | userPrompt(String, optional) | 用户提示文本 |
| 传出参数 | code(int), message(String), data{list{name, quantity}} | 识别出的食材列表 |
| 返回code | 200成功, 500识别失败 | 识别结果状态码 |

#### 3.1.3 食谱管理模块接口设计

食谱管理模块提供食谱生成、详情查看、删除和更新功能。

**主要接口设计:**

| 接口名称 | 请求方式 | 传入参数 | 传出参数 | 功能说明 |
|---------|---------|---------|---------|---------|
| /api/recipe/generate | POST | userId, foodIds, expectCount | code, message, data{list} | 生成食谱 |
| /api/recipe/list | GET | userId | code, message, data(list) | 获取食谱列表 |
| /api/recipe/detail | GET | id | code, message, data(recipe) | 获取食谱详情 |
| /api/recipe/delete | DELETE | id | code, message | 删除食谱 |
| /api/recipe/save | POST | recipe对象 | code, message | 保存食谱 |
| /api/recipe/update | PUT | recipe对象 | code, message | 更新食谱 |
| /api/recipe/generateImage | POST | id | code, message, data{imageUrl} | 生成食谱图片 |

**食谱生成接口详细设计表:**

| 字段 | 类型 | 说明 |
|------|------|------|
| 接口名 | generateRecipe | 生成食谱 |
| 请求URL | /api/recipe/generate | POST |
| 传入参数 | userId(Long), foodIds(List<Long>), expectCount(int) | 用户ID、食材ID列表、期望数量 |
| 传出参数 | code(int), message(String), data{list{recipes}} | 状态码、消息、食谱列表 |
| 返回code | 200成功, 500AI调用失败 | 生成结果状态码 |
| 特殊处理 | 考虑用户饮食偏好、调用MiniMax API生成 | - |

**食谱详情接口详细设计表:**

| 字段 | 类型 | 说明 |
|------|------|------|
| 接口名 | getRecipeDetail | 获取食谱详情 |
| 请求URL | /api/recipe/detail | GET |
| 传入参数 | id(Long) | 食谱ID |
| 传出参数 | code(int), message(String), data{recipe对象} | 状态码、消息、食谱详情 |
| 返回code | 200成功, 404不存在 | 查询结果状态码 |
| 包含字段 | name, englishName, imageKeyword, imageUrl, cookingTime, difficultyLevel, steps | - |

#### 3.1.4 收藏管理模块接口设计

收藏管理模块提供食谱的收藏和取消收藏功能。

**主要接口设计:**

| 接口名称 | 请求方式 | 传入参数 | 传出参数 | 功能说明 |
|---------|---------|---------|---------|---------|
| /api/recipe/collectPersonal | POST | recipeId, userId | code, message | 收藏食谱(个人) |
| /api/recipe/collected | GET | userId | code, message, data(list) | 获取收藏列表 |
| /api/recipe/uncollect | POST | recipeId, userId | code, message | 取消收藏 |

**收藏接口详细设计表:**

| 字段 | 类型 | 说明 |
|------|------|------|
| 接口名 | collectPersonal | 个人收藏食谱 |
| 请求URL | /api/recipe/collectPersonal | POST |
| 传入参数 | recipeId(Long), userId(Long) | 食谱ID、用户ID |
| 传出参数 | code(int), message(String) | 状态码、消息 |
| 返回code | 200成功, 400失败(已收藏等) | 收藏结果状态码 |
| 业务规则 | 检查是否已收藏,防止重复收藏 | - |

#### 3.1.5 营养分析模块接口设计

营养分析模块提供食谱的营养成分计算和健康评估功能。

**主要接口设计:**

| 接口名称 | 请求方式 | 传入参数 | 传出参数 | 功能说明 |
|---------|---------|---------|---------|---------|
| /api/nutrition/analyze | POST | recipeId | code, message, data | 分析食谱营养 |
| /api/nutrition/batchAnalyze | POST | recipeIds | code, message, data(list) | 批量分析营养 |

**营养分析接口详细设计表:**

| 字段 | 类型 | 说明 |
|------|------|------|
| 接口名 | analyzeNutrition | 分析食谱营养 |
| 请求URL | /api/nutrition/analyze | POST |
| 传入参数 | recipeId(Long), recipeSteps(String), foodNames(List<String>) | 食谱ID、步骤、食材列表 |
| 传出参数 | code(int), message, data{nutritionData, evaluation, suggestion} | 状态码、营养数据、评估、建议 |
| 返回code | 200成功, 500分析失败 | 分析结果状态码 |
| nutritionData | {calorie, protein, fat, carbohydrate, vitamin, mineral} | 营养成分数据 |

### 3.2 数据库设计

#### 3.2.1 数据库概念模型设计

**3.2.1.1 数据库E-R图设计**

根据系统需求分析,本系统设计了6个主要实体,分别是用户实体、用户偏好实体、食材实体、食谱实体、收藏实体和营养实体。系统总体E-R图如图3-1所示:

```
                    ┌──────────────┐
                    │     用户      │
                    │   (user)     │
                    └──────────────┘
                           │
                           │ 1:N
                           ▼
┌──────────────┐     ┌──────────────┐
│  用户偏好     │     │    食材      │
│ (preference) │     │(food_material)│
└──────────────┘     └──────────────┘
                           │
                           │ N:1
                           ▼
                    ┌──────────────┐
                    │    食谱       │
                    │   (recipe)   │
                    └──────────────┘
                           │
                           │ N:N (通过收藏表)
                           ▼
                    ┌──────────────┐
                    │    收藏       │
                    │ (collection)  │
                    └──────────────┘

                    ┌──────────────┐
                    │    营养       │
                    │ (nutrition)   │
                    └──────────────┘
                           ▲
                           │ N:1
                    ┌──────────────┘
                    │    食材       │
                    │(food_material)│
                    └──────────────┘
```

图3-1 系统总体E-R图

**3.2.1.2 数据库实体模型**

**1. 用户实体模型**

用户实体是系统的基础实体,存储用户的基本信息。用户实体模型图如图3-2所示:

```
        ┌─────────────────────┐
        │        用户          │
        │      (user)         │
        ├─────────────────────┤
        │ id: INT (PK)        │
        │ username: VARCHAR   │
        │ password: VARCHAR   │
        │ age: INT            │
        │ gender: CHAR        │
        │ phone: VARCHAR      │
        │ role: VARCHAR        │
        │ create_time: DATETIME│
        └─────────────────────┘
```

图3-2 用户实体模型图

用户实体的属性包括:
- **id**: 用户唯一标识,主键自增
- **username**: 用户名,唯一,用于登录
- **password**: 密码,BCrypt加密存储
- **age**: 年龄,可为空
- **gender**: 性别
- **phone**: 手机号
- **role**: 角色(USER/ADMIN)
- **create_time**: 创建时间

**2. 用户偏好实体模型**

用户偏好实体存储用户的个性化饮食设置,与用户实体一对一关联。用户偏好实体模型图如图3-3所示:

```
        ┌─────────────────────┐
        │     用户偏好         │
        │ (user_preference)   │
        ├─────────────────────┤
        │ id: INT (PK)        │
        │ user_id: INT (FK)   │
        │ health_goal: VARCHAR│
        │ diet_limit: VARCHAR │
        │ taste_preference:VARCHAR│
        │ cooking_level: VARCHAR│
        └─────────────────────┘
```

图3-3 用户偏好实体模型图

用户偏好实体的属性包括:
- **id**: 偏好唯一标识,主键自增
- **user_id**: 用户ID,外键关联用户表
- **health_goal**: 健康目标(减脂/控糖/增肌/普通)
- **diet_limit**: 饮食限制(过敏原/素食等)
- **taste_preference**: 口味偏好(辣/淡/甜等)
- **cooking_level**: 烹饪水平(新手/进阶/熟练)

**3. 食材实体模型**

食材实体存储用户添加的食材信息。食材实体模型图如图3-4所示:

```
        ┌─────────────────────────┐
        │        食材              │
        │   (food_material)       │
        ├─────────────────────────┤
        │ id: INT (PK)            │
        │ name: VARCHAR           │
        │ type: VARCHAR           │
        │ quantity: DECIMAL       │
        │ shelf_life: INT         │
        │ nutrition_id: INT (FK)  │
        │ user_id: INT (FK)      │
        │ create_time: DATETIME   │
        └─────────────────────────┘
```

图3-4 食材实体模型图

食材实体的属性包括:
- **id**: 食材唯一标识,主键自增
- **name**: 食材名称
- **type**: 食材类型(蔬菜/肉类/水产/豆制品等)
- **quantity**: 数量(克)
- **shelf_life**: 保质期(天)
- **nutrition_id**: 关联营养数据ID
- **user_id**: 所属用户ID
- **create_time**: 创建时间

**4. 食谱实体模型**

食谱实体存储AI生成的食谱信息。食谱实体模型图如图3-5所示:

```
        ┌─────────────────────────┐
        │        食谱              │
        │      (recipe)            │
        ├─────────────────────────┤
        │ id: INT (PK)            │
        │ name: VARCHAR           │
        │ english_name: VARCHAR   │
        │ image_keyword: VARCHAR  │
        │ image_url: VARCHAR      │
        │ user_id: INT (FK)       │
        │ cooking_time: INT       │
        │ difficulty_level: VARCHAR│
        │ steps: TEXT             │
        │ food_ids: VARCHAR       │
        │ collect_count: INT       │
        │ create_time: DATETIME   │
        └─────────────────────────┘
```

图3-5 食谱实体模型图

食谱实体的属性包括:
- **id**: 食谱唯一标识,主键自增
- **name**: 中文食谱名称
- **english_name**: 英文名称(用于图片搜索)
- **image_keyword**: 图片搜索关键词
- **image_url**: AI生成的图片URL或本地路径
- **user_id**: 所属用户ID
- **cooking_time**: 烹饪时间(分钟)
- **difficulty_level**: 难度等级(简单/中等/困难)
- **steps**: 详细烹饪步骤
- **food_ids**: 使用的食材ID列表
- **collect_count**: 被收藏次数
- **create_time**: 创建时间

**5. 收藏实体模型**

收藏实体存储用户收藏的食谱关联。收藏实体模型图如图3-6所示:

```
        ┌─────────────────────────┐
        │        收藏              │
        │   (user_collection)      │
        ├─────────────────────────┤
        │ id: INT (PK)            │
        │ user_id: INT (FK)       │
        │ recipe_id: INT (FK)     │
        │ create_time: DATETIME   │
        └─────────────────────────┘
```

图3-6 收藏实体模型图

收藏实体的属性包括:
- **id**: 收藏记录唯一标识,主键自增
- **user_id**: 用户ID
- **recipe_id**: 食谱ID
- **create_time**: 收藏时间

唯一约束: (user_id, recipe_id) 防止重复收藏

**6. 营养实体模型**

营养实体存储食材的营养成分数据。营养实体模型图如图3-7所示:

```
        ┌─────────────────────────┐
        │        营养              │
        │     (nutrition)          │
        ├─────────────────────────┤
        │ id: INT (PK)            │
        │ food_id: INT (FK)       │
        │ calorie: DECIMAL        │
        │ protein: DECIMAL         │
        │ fat: DECIMAL            │
        │ carbohydrate: DECIMAL   │
        │ vitamin: VARCHAR        │
        │ mineral: VARCHAR        │
        └─────────────────────────┘
```

图3-7 营养实体模型图

营养实体的属性包括:
- **id**: 营养数据唯一标识,主键自增
- **food_id**: 关联食材ID
- **calorie**: 热量(大卡/100g)
- **protein**: 蛋白质(g/100g)
- **fat**: 脂肪(g/100g)
- **carbohydrate**: 碳水化合物(g/100g)
- **vitamin**: 维生素含量描述
- **mineral**: 矿物质含量描述

#### 3.2.2 数据库物理模型设计

根据概念模型,设计以下数据库表结构:

**1. 用户表 (user)**

| 字段名 | 类型 | 长度 | 允许空 | 主外键 | 说明 |
|--------|------|------|--------|--------|------|
| id | INT | 11 | 不空 | 主键 | 用户ID,自增 |
| username | VARCHAR | 50 | 不空 | 唯一键 | 用户名 |
| password | VARCHAR | 100 | 不空 | - | 加密后密码 |
| age | INT | 3 | 空 | - | 年龄 |
| gender | CHAR | 10 | 空 | - | 性别 |
| phone | VARCHAR | 11 | 空 | 唯一键 | 手机号 |
| role | VARCHAR | 20 | 不空 | - | 角色:USER/ADMIN |
| create_time | DATETIME | - | 不空 | - | 创建时间 |

```sql
CREATE TABLE `user` (
    `id` INT(11) NOT NULL AUTO_INCREMENT COMMENT '用户唯一标识',
    `username` VARCHAR(50) NOT NULL COMMENT '用户名',
    `password` VARCHAR(100) NOT NULL COMMENT '加密存储',
    `age` INT(3) DEFAULT NULL COMMENT '用户年龄',
    `gender` CHAR(10) DEFAULT NULL COMMENT '用户性别',
    `phone` VARCHAR(11) DEFAULT NULL COMMENT '手机号码',
    `role` VARCHAR(20) NOT NULL DEFAULT 'USER' COMMENT '用户角色：USER=普通用户，ADMIN=管理员',
    `create_time` DATETIME NOT NULL COMMENT '创建时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_username` (`username`),
    UNIQUE KEY `uk_phone` (`phone`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

**2. 用户偏好表 (user_preference)**

| 字段名 | 类型 | 长度 | 允许空 | 主外键 | 说明 |
|--------|------|------|--------|--------|------|
| id | INT | 11 | 不空 | 主键 | 偏好ID,自增 |
| user_id | INT | 11 | 不空 | 外键 | 关联用户 |
| health_goal | VARCHAR | 50 | 不空 | - | 健康目标 |
| diet_limit | VARCHAR | 100 | 空 | - | 饮食限制 |
| taste_preference | VARCHAR | 50 | 不空 | - | 口味偏好 |
| cooking_level | VARCHAR | 20 | 不空 | - | 烹饪熟练度 |

```sql
CREATE TABLE `user_preference` (
    `id` INT(11) NOT NULL AUTO_INCREMENT COMMENT '偏好唯一标识',
    `user_id` INT(11) NOT NULL COMMENT '关联用户',
    `health_goal` VARCHAR(50) NOT NULL COMMENT '健康目标（减脂/控糖等）',
    `diet_limit` VARCHAR(100) DEFAULT NULL COMMENT '饮食限制（过敏原/素食等）',
    `taste_preference` VARCHAR(50) NOT NULL COMMENT '口味倾向（辣/淡等）',
    `cooking_level` VARCHAR(20) NOT NULL COMMENT '烹饪熟练度（新手/进阶等）',
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`),
    CONSTRAINT `fk_preference_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户偏好表';
```

**3. 营养数据表 (nutrition)**

| 字段名 | 类型 | 长度 | 允许空 | 主外键 | 说明 |
|--------|------|------|--------|--------|------|
| id | INT | 11 | 不空 | 主键 | 营养ID,自增 |
| food_id | INT | 11 | 空 | 外键 | 关联食材 |
| calorie | DECIMAL | 10,2 | 不空 | - | 热量 |
| protein | DECIMAL | 10,2 | 不空 | - | 蛋白质 |
| fat | DECIMAL | 10,2 | 不空 | - | 脂肪 |
| carbohydrate | DECIMAL | 10,2 | 不空 | - | 碳水化合物 |
| vitamin | VARCHAR | 100 | 空 | - | 维生素 |
| mineral | VARCHAR | 100 | 空 | - | 矿物质 |

```sql
CREATE TABLE `nutrition` (
    `id` INT(11) NOT NULL AUTO_INCREMENT COMMENT '营养唯一标识',
    `food_id` INT(11) DEFAULT NULL COMMENT '关联食材',
    `calorie` DECIMAL(10,2) NOT NULL COMMENT '热量（大卡/100g）',
    `protein` DECIMAL(10,2) NOT NULL COMMENT '蛋白质（g/100g）',
    `fat` DECIMAL(10,2) NOT NULL COMMENT '脂肪（g/100g）',
    `carbohydrate` DECIMAL(10,2) NOT NULL COMMENT '碳水化合物（g/100g）',
    `vitamin` VARCHAR(100) DEFAULT NULL COMMENT '维生素含量',
    `mineral` VARCHAR(100) DEFAULT NULL COMMENT '矿物质含量',
    PRIMARY KEY (`id`),
    KEY `idx_food_id` (`food_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='营养数据表';
```

**4. 食材表 (food_material)**

| 字段名 | 类型 | 长度 | 允许空 | 主外键 | 说明 |
|--------|------|------|--------|--------|------|
| id | INT | 11 | 不空 | 主键 | 食材ID,自增 |
| name | VARCHAR | 50 | 不空 | - | 食材名称 |
| type | VARCHAR | 20 | 不空 | - | 食材类别 |
| quantity | DECIMAL | 10,2 | 不空 | - | 食材数量(g) |
| shelf_life | INT | 5 | 空 | - | 保质期(天) |
| nutrition_id | INT | 11 | 空 | 外键 | 关联营养数据 |
| user_id | INT | 11 | 空 | 外键 | 所属用户 |
| create_time | DATETIME | - | 不空 | - | 创建时间 |

```sql
CREATE TABLE `food_material` (
    `id` INT(11) NOT NULL AUTO_INCREMENT COMMENT '食材唯一标识',
    `name` VARCHAR(50) NOT NULL COMMENT '食材名称',
    `type` VARCHAR(20) NOT NULL COMMENT '食材类别（蔬菜/肉类等）',
    `quantity` DECIMAL(10,2) NOT NULL COMMENT '食材数量',
    `shelf_life` INT(5) DEFAULT NULL COMMENT '保质期（天）',
    `nutrition_id` INT(11) DEFAULT NULL COMMENT '关联营养数据',
    `user_id` INT(11) DEFAULT NULL COMMENT '所属用户',
    `create_time` DATETIME NOT NULL COMMENT '创建时间',
    PRIMARY KEY (`id`),
    KEY `idx_type` (`type`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_nutrition_id` (`nutrition_id`),
    CONSTRAINT `fk_food_nutrition` FOREIGN KEY (`nutrition_id`) REFERENCES `nutrition` (`id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='食材表';
```

**5. 食谱表 (recipe)**

| 字段名 | 类型 | 长度 | 允许空 | 主外键 | 说明 |
|--------|------|------|--------|--------|------|
| id | INT | 11 | 不空 | 主键 | 食谱ID,自增 |
| name | VARCHAR | 100 | 不空 | - | 食谱名称 |
| english_name | VARCHAR | 200 | 空 | - | 英文名称 |
| image_keyword | VARCHAR | 200 | 空 | - | 图片搜索关键词 |
| image_url | VARCHAR | 500 | 空 | - | AI图片URL/本地路径 |
| user_id | INT | 11 | 不空 | 外键 | 所属用户 |
| cooking_time | INT | 5 | 不空 | - | 烹饪时间(分钟) |
| difficulty_level | VARCHAR | 20 | 不空 | - | 难度等级 |
| steps | TEXT | - | 不空 | - | 烹饪步骤 |
| food_ids | VARCHAR | 255 | 空 | - | 食材ID列表 |
| collect_count | INT | 11 | 不空 | - | 收藏次数 |
| create_time | DATETIME | - | 不空 | - | 创建时间 |

```sql
CREATE TABLE `recipe` (
    `id` INT(11) NOT NULL AUTO_INCREMENT COMMENT '食谱唯一标识',
    `name` VARCHAR(100) NOT NULL COMMENT '食谱名称',
    `english_name` VARCHAR(200) DEFAULT NULL COMMENT '英文名称',
    `image_keyword` VARCHAR(200) DEFAULT NULL COMMENT '图片搜索关键词',
    `image_url` VARCHAR(500) DEFAULT NULL COMMENT 'AI图片URL/本地路径',
    `user_id` INT(11) NOT NULL COMMENT '关联用户',
    `cooking_time` INT(5) NOT NULL COMMENT '烹饪耗时（分钟）',
    `difficulty_level` VARCHAR(20) NOT NULL COMMENT '难度等级',
    `steps` TEXT NOT NULL COMMENT '烹饪步骤',
    `food_ids` VARCHAR(255) DEFAULT NULL COMMENT '食材ID列表',
    `collect_count` INT(11) NOT NULL DEFAULT 0 COMMENT '收藏次数',
    `create_time` DATETIME NOT NULL COMMENT '创建时间',
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_difficulty` (`difficulty_level`),
    CONSTRAINT `fk_recipe_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='食谱表';
```

**6. 用户收藏表 (user_collection)**

| 字段名 | 类型 | 长度 | 允许空 | 主外键 | 说明 |
|--------|------|------|--------|--------|------|
| id | INT | 11 | 不空 | 主键 | 收藏ID,自增 |
| user_id | INT | 11 | 不空 | 外键 | 关联用户 |
| recipe_id | INT | 11 | 不空 | 外键 | 关联食谱 |
| create_time | DATETIME | - | 不空 | - | 收藏时间 |

```sql
CREATE TABLE `user_collection` (
    `id` INT(11) NOT NULL AUTO_INCREMENT COMMENT '收藏ID',
    `user_id` INT(11) NOT NULL COMMENT '关联用户',
    `recipe_id` INT(11) NOT NULL COMMENT '关联食谱',
    `create_time` DATETIME NOT NULL COMMENT '收藏时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_recipe` (`user_id`, `recipe_id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_recipe_id` (`recipe_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户收藏表';
```

---

## 第4章 系统实现与测试

### 4.1 核心功能实现

#### 4.1.1 AI食谱生成实现

食谱生成是系统的核心功能,通过调用大语言模型API实现。核心实现类为`AiApiClient`,主要方法为`generateRecipe`。

**4.1.1.1 AI客户端实现**

AI客户端封装了对阿里云百炼平台API的调用,核心代码结构如下:

```java
@Component
public class AiApiClient {
    @Value("${ai.api-key}")
    private String apiKey;

    @Value("${ai.base-url}")
    private String baseUrl;

    private final HttpClient httpClient = HttpClient.newBuilder()
            .connectTimeout(Duration.ofSeconds(30))
            .build();

    /**
     * 生成食谱
     */
    public List<Recipe> generateRecipe(List<String> foodNames, Long userId,
            int expectCount, String difficultyLevel, String tastePreference,
            String cookingLevel, Integer maxCookingTime, String dietLimit, String healthGoal) {

        // 构建系统提示词
        StringBuilder systemPrompt = new StringBuilder();
        systemPrompt.append("你是一个专业厨师和食谱设计师。根据用户提供的食材，生成个性化食谱。");
        systemPrompt.append("请以JSON数组格式返回，每个食谱包含：");
        systemPrompt.append("name(中文食谱名称)、");
        systemPrompt.append("englishName(英文食谱名称),");
        systemPrompt.append("imageKeyword(图片搜索关键词),");
        systemPrompt.append("cookingTime(烹饪时间分钟数)、");
        systemPrompt.append("difficultyLevel(难度：简单/中等/困难)、");
        systemPrompt.append("steps(详细烹饪步骤)、");
        systemPrompt.append("foodIds(使用的食材名称列表)。");

        // 构建用户提示词
        StringBuilder userPrompt = new StringBuilder();
        userPrompt.append("现有食材：").append(String.join("、", foodNames)).append("\n");
        userPrompt.append("请生成").append(expectCount).append("个食谱。\n");

        // 添加用户偏好信息
        if (healthGoal != null && !healthGoal.isEmpty()) {
            userPrompt.append("健康目标：").append(healthGoal).append("\n");
        }
        if (dietLimit != null && !dietLimit.isEmpty()) {
            userPrompt.append("饮食限制：").append(dietLimit).append("\n");
        }
        if (tastePreference != null) {
            userPrompt.append("口味偏好：").append(tastePreference).append("\n");
        }

        // 调用AI API
        String aiResponse = callAiApi(systemPrompt.toString(), userPrompt.toString());
        return parseRecipeListFromJson(extractJson(aiResponse), userId);
    }

    /**
     * 调用AI API
     */
    private String callAiApi(String systemPrompt, String userPrompt) {
        JSONObject requestBody = new JSONObject();
        requestBody.put("model", "MiniMax-M2.7");
        requestBody.put("temperature", 0.7);

        JSONArray messages = new JSONArray();
        // 添加系统消息
        JSONObject systemMsg = new JSONObject();
        systemMsg.put("role", "system");
        systemMsg.put("content", systemPrompt);
        messages.add(systemMsg);

        // 添加用户消息
        JSONObject userMsg = new JSONObject();
        userMsg.put("role", "user");
        userMsg.put("content", userPrompt);
        messages.add(userMsg);

        requestBody.put("messages", messages);

        HttpRequest request = HttpRequest.newBuilder()
                .uri(URI.create(baseUrl + "/chat/completions"))
                .header("Content-Type", "application/json")
                .header("Authorization", "Bearer " + apiKey)
                .timeout(Duration.ofSeconds(60))
                .POST(HttpRequest.BodyPublishers.ofString(requestBody.toJSONString()))
                .build();

        HttpResponse<String> response = httpClient.send(request,
            HttpResponse.BodyHandlers.ofString());

        if (response.statusCode() == 200) {
            JSONObject respJson = JSON.parseObject(response.body());
            JSONArray choices = respJson.getJSONArray("choices");
            return choices.getJSONObject(0).getJSONObject("message").getString("content");
        }
        throw new RuntimeException("AI API调用失败");
    }
}
```

**4.1.1.2 JSON响应解析**

AI返回的是JSON格式的食谱列表,需要进行解析:

```java
private List<Recipe> parseRecipeListFromJson(String json, Long userId) {
    List<Recipe> recipeList = new ArrayList<>();
    try {
        JSONArray arr = JSON.parseArray(json);
        for (int i = 0; i < arr.size(); i++) {
            JSONObject obj = arr.getJSONObject(i);
            Recipe recipe = new Recipe();
            recipe.setName(obj.getString("name"));
            recipe.setEnglishName(obj.getString("englishName"));
            recipe.setImageKeyword(obj.getString("imageKeyword"));
            recipe.setUserId(userId);
            recipe.setCookingTime(obj.getInteger("cookingTime"));
            recipe.setDifficultyLevel(obj.getString("difficultyLevel"));
            recipe.setSteps(obj.getString("steps"));
            recipe.setFoodIds(obj.getString("foodIds"));
            recipe.setCollectCount(0);
            recipeList.add(recipe);
        }
    } catch (Exception e) {
        throw new RuntimeException("JSON解析失败: " + e.getMessage());
    }
    return recipeList;
}
```

**4.1.1.3 食谱生成服务实现**

```java
@Service
public class RecipeServiceImpl implements RecipeService {

    @Autowired
    private RecipeMapper recipeMapper;

    @Autowired
    private FoodMapper foodMapper;

    @Autowired
    private AiApiClient aiApiClient;

    @Override
    public List<Recipe> generateRecipe(List<Long> foodIds, Long userId, int expectCount) {
        // 1. 获取用户偏好
        UserPreference preference = userPreferenceMapper.selectByUserId(userId);

        // 2. 获取食材名称列表
        List<String> foodNames = foodMapper.selectByIds(foodIds)
            .stream()
            .map(FoodMaterial::getName)
            .collect(Collectors.toList());

        // 3. 调用AI生成食谱
        List<Recipe> recipes = aiApiClient.generateRecipe(
            foodNames, userId, expectCount,
            null, // difficultyLevel
            preference != null ? preference.getTastePreference() : null,
            preference != null ? preference.getCookingLevel() : null,
            null, // maxCookingTime
            preference != null ? preference.getDietLimit() : null,
            preference != null ? preference.getHealthGoal() : null
        );

        // 4. 保存到数据库
        for (Recipe recipe : recipes) {
            recipeMapper.insert(recipe);
        }

        return recipes;
    }
}
```

#### 4.1.2 图片识别实现

图片识别功能允许用户通过上传食材图片来自动识别食材种类。

**4.1.2.1 图片识别API调用**

```java
/**
 * 使用多模态模型识别图片中的食材
 */
public String recognizeImageFood(String imageDataUrl, String userPrompt) {
    JSONObject requestBody = new JSONObject();
    requestBody.put("model", "MiniMax-M2.7");

    JSONArray messages = new JSONArray();

    // 用户消息(包含图片)
    JSONObject userMsg = new JSONObject();
    JSONArray contentArray = new JSONArray();

    // 文本部分
    JSONObject textPart = new JSONObject();
    textPart.put("type", "text");
    textPart.put("text", userPrompt != null ? userPrompt :
        "请识别图片中的食材,列出每种食材的名称和数量(克)");
    contentArray.add(textPart);

    // 图片部分
    JSONObject imagePart = new JSONObject();
    imagePart.put("type", "image_url");
    JSONObject imageUrl = new JSONObject();
    imageUrl.put("url", imageDataUrl);
    imageUrl.put("detail", "high");
    imagePart.put("image_url", imageUrl);
    contentArray.add(imagePart);

    userMsg.put("role", "user");
    userMsg.put("content", contentArray);
    messages.add(userMsg);

    requestBody.put("messages", messages);

    // 发送请求并解析响应...
}
```

**4.1.2.2 前端图片上传实现**

```javascript
// Vue组件中处理图片上传
async uploadAndRecognize(file) {
    // 将图片转为base64
    const base64 = await this.fileToBase64(file)

    // 调用识别API
    const res = await fetch('/api/food/recognizeImage', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ imageData: base64 })
    })

    const data = await res.json()
    if (data.code === 200) {
        // 显示识别结果,供用户确认
        this.recognizedFoods = data.data
    }
}

fileToBase64(file) {
    return new Promise((resolve, reject) => {
        const reader = new FileReader()
        reader.onload = () => resolve(reader.result)
        reader.onerror = reject
        reader.readAsDataURL(file)
    })
}
```

#### 4.1.3 AI图片生成实现

图片生成功能使用MiniMax image-01模型为食谱生成匹配的美食图片。

**4.1.3.1 图片生成API调用**

```java
/**
 * 使用MiniMax image-01模型生成食谱图片
 */
public String generateRecipeImage(String recipeName, String englishName, String prompt) {
    JSONObject requestBody = new JSONObject();
    requestBody.put("model", "image-01");

    // 构建图片生成提示词
    String imagePrompt = String.format(
        "A professional food photography of %s, Chinese dish, " +
        "appetizing presentation, restaurant style, top-down angle, " +
        "natural lighting, 4K quality",
        englishName != null && !englishName.isEmpty() ? englishName : recipeName,
        recipeName
    );

    requestBody.put("prompt", imagePrompt);
    requestBody.put("aspect_ratio", "1:1");
    requestBody.put("response_format", "url");
    requestBody.put("n", 1);
    requestBody.put("prompt_optimizer", true);

    HttpRequest request = HttpRequest.newBuilder()
            .uri(URI.create(baseUrl + "/image_generation"))
            .header("Content-Type", "application/json")
            .header("Authorization", "Bearer " + apiKey)
            .timeout(Duration.ofSeconds(120))
            .POST(HttpRequest.BodyPublishers.ofString(requestBody.toJSONString()))
            .build();

    HttpResponse<String> response = httpClient.send(request,
        HttpResponse.BodyHandlers.ofString());

    if (response.statusCode() == 200) {
        JSONObject respJson = JSON.parseObject(response.body());
        JSONObject data = respJson.getJSONObject("data");
        JSONArray imageUrls = data.getJSONArray("image_urls");
        return imageUrls.getString(0);
    }
    throw new RuntimeException("图片生成失败");
}
```

**4.1.3.2 图片本地保存实现**

```java
@Component
public class ImageDownloader {

    private static final String LOCAL_PICTURE_PATH = "C:/develop/codeBase/bsfood1/picture";

    /**
     * 下载图片并保存到本地
     */
    public String downloadAndSaveImage(String imageUrl, String fileName) {
        if (imageUrl == null || imageUrl.isEmpty()) {
            return null;
        }

        // 确保目录存在
        Path dirPath = Paths.get(LOCAL_PICTURE_PATH);
        if (!Files.exists(dirPath)) {
            Files.createDirectories(dirPath);
        }

        // 清理文件名
        String cleanFileName = cleanFileName(fileName);
        String savedPath = LOCAL_PICTURE_PATH + "/" + cleanFileName + ".jpg";

        // 如果文件已存在,直接返回
        if (Files.exists(Paths.get(savedPath))) {
            return savedPath;
        }

        // 下载图片
        HttpRequest request = HttpRequest.newBuilder()
                .uri(URI.create(imageUrl))
                .header("User-Agent", "Mozilla/5.0")
                .GET()
                .build();

        HttpResponse<byte[]> response = httpClient.send(request,
            HttpResponse.BodyHandlers.ofByteArray());

        if (response.statusCode() == 200) {
            Files.write(Paths.get(savedPath), response.body());
            return savedPath;
        }
        return null;
    }

    private String cleanFileName(String fileName) {
        return fileName.replaceAll("[\\\\/:*?\"<>|]", "_")
                      .replaceAll("\\s+", "_");
    }
}
```

**4.1.3.3 图片生成控制器实现**

```java
@PostMapping("/generateImage")
public Map<String, Object> generateImage(@RequestParam Long id) {
    Map<String, Object> result = new HashMap<>();
    try {
        Recipe recipe = recipeService.getRecipeById(id);
        if (recipe == null) {
            result.put("code", 404);
            result.put("message", "食谱不存在");
            return result;
        }

        // 1. 检查本地是否已有同名图片
        String localPath = imageDownloader.getLocalImagePath(recipe.getName());
        if (localPath != null) {
            String imageUrl = "/picture/" + recipe.getName().replaceAll(...) + ".jpg";
            recipe.setImageUrl(imageUrl);
            recipeService.updateRecipe(recipe);
            result.put("code", 200);
            result.put("message", "使用已有图片");
            result.put("data", Map.of("imageUrl", imageUrl));
            return result;
        }

        // 2. 调用MiniMax生成图片
        String aiImageUrl = aiApiClient.generateRecipeImage(
            recipe.getName(), recipe.getEnglishName(), recipe.getImageKeyword());

        // 3. 下载并保存到本地
        String savedPath = imageDownloader.downloadAndSaveImage(aiImageUrl, recipe.getName());

        if (savedPath != null) {
            String imageUrl = "/picture/" + recipe.getName().replaceAll(...) + ".jpg";
            recipe.setImageUrl(imageUrl);
            recipeService.updateRecipe(recipe);
            result.put("code", 200);
            result.put("message", "图片生成并保存成功");
            result.put("data", Map.of("imageUrl", imageUrl));
        }
    } catch (Exception e) {
        result.put("code", 500);
        result.put("message", "图片生成失败: " + e.getMessage());
    }
    return result;
}
```

### 4.2 系统实现效果

系统实现后的主要界面效果如下:

**1. 系统登录页面**

用户输入用户名和密码进行登录,系统采用JWT Token进行身份认证。登录页面简洁美观,符合现代审美,实现效果如图4-1所示:

```
┌─────────────────────────────────────────────────┐
│                                                  │
│            基于大模型的个性化食谱生成系统            │
│                                                  │
│         ┌─────────────────────────────────┐      │
│         │  用户名: ___________________     │      │
│         │  密码:   ___________________     │      │
│         │                                 │      │
│         │       [    登 录    ]          │      │
│         │       [    注 册    ]          │      │
│         └─────────────────────────────────┘      │
│                                                  │
└─────────────────────────────────────────────────┘
```

图4-1 系统登录页面

**2. 食材管理页面**

用户可以在食材管理页面查看已添加的食材,支持文本输入添加和图片识别添加两种方式。实现效果如图4-2所示:

```
┌─────────────────────────────────────────────────┐
│  食材管理                    [添加食材] [图片识别] │
├─────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────┐   │
│  │  蔬菜类                                  │   │
│  │  ├─ 番茄 (500g)    保质期: 7天  [编辑][删除] │   │
│  │  ├─ 土豆 (300g)    保质期: 30天 [编辑][删除] │   │
│  │  └─ 青椒 (200g)    保质期: 5天  [编辑][删除] │   │
│  ├──────────────────────────────────────────┤   │
│  │  肉类                                    │   │
│  │  ├─ 鸡腿 (400g)    保质期: 3天  [编辑][删除] │   │
│  │  └─ 猪肉 (500g)    保质期: 5天  [编辑][删除] │   │
│  └──────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

图4-2 食材管理页面

**3. 食谱生成页面**

用户选择食材后点击生成按钮,系统调用AI生成个性化食谱。实现效果如图4-3所示:

```
┌─────────────────────────────────────────────────┐
│  食谱生成                                         │
├─────────────────────────────────────────────────┤
│  选择食材: [番茄 x ] [土豆 x ] [鸡腿 x ] [+更多]   │
│  食谱数量: [3] 个                                │
│                                                  │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐   │
│  │ 番茄炒蛋    │ │ 土豆烧鸡腿  │ │ 咖喱鸡肉饭  │   │
│  │ ⏱ 15分钟   │ │ ⏱ 30分钟   │ │ ⏱ 45分钟   │   │
│  │ 难度: 简单  │ │ 难度: 中等  │ │ 难度: 中等  │   │
│  │            │ │            │ │            │   │
│  │ [查看详情]  │ │ [查看详情]  │ │ [查看详情]  │   │
│  │ [收藏]      │ │ [收藏]      │ │ [收藏]      │   │
│  └────────────┘ └────────────┘ └────────────┘   │
└─────────────────────────────────────────────────┘
```

图4-3 食谱生成页面

**4. 食谱详情页面**

点击食谱卡片可查看详细信息,包括AI生成的美食图片、烹饪步骤等。实现效果如图4-4所示:

```
┌─────────────────────────────────────────────────┐
│  食谱详情                              [🔄 换一张] │
├─────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────────────────┐  │
│  │              │  │  番茄炒蛋               │  │
│  │   [美食图片]  │  │  ⏱ 约 15 分钟           │  │
│  │              │  │  难度: 简单             │  │
│  └──────────────┘  └──────────────────────────┘  │
│                                                  │
│  烹饪步骤:                                       │
│  1. 番茄洗净切块,鸡蛋打散加少许盐搅拌均匀。          │
│  2. 热锅凉油,先将鸡蛋液炒至凝固盛出。              │
│  3. 锅中再加少许油,放入番茄块翻炒出汁。            │
│  4. 加入炒好的鸡蛋,加盐、糖调味,翻炒均匀即可。      │
│                                                  │
│           [收藏此食谱]  [营养分析]                 │
└─────────────────────────────────────────────────┘
```

图4-4 食谱详情页面

**5. 营养分析页面**

点击"营养分析"按钮可查看食谱的营养成分和健康评估。实现效果如图4-5所示:

```
┌─────────────────────────────────────────────────┐
│  营养分析                                         │
├─────────────────────────────────────────────────┤
│  食谱: 番茄炒蛋                                    │
│                                                  │
│  ┌─────────────────────────────────────────────┐ │
│  │  热量: 185 大卡    蛋白质: 12.5g          │ │
│  │  脂肪: 10.2g      碳水: 8.5g              │ │
│  └─────────────────────────────────────────────┘ │
│                                                  │
│  营养评估:                                        │
│  该食谱热量适中,蛋白质含量较高,适合减脂期间食用。    │
│  脂肪含量略高,建议减少用油量。                      │
│                                                  │
│  改善建议:                                        │
│  · 减少食用油用量,可降低约30%脂肪摄入             │
│  · 可增加蔬菜配比,提高膳食纤维含量                 │
│  · 少放盐,有助于控制钠摄入                        │
└─────────────────────────────────────────────────┘
```

图4-5 营养分析页面

**6. 个人中心页面**

用户可以在个人中心管理自己的信息和偏好设置。实现效果如图4-6所示:

```
┌─────────────────────────────────────────────────┐
│  个人中心                                         │
├─────────────────────────────────────────────────┤
│  用户信息                    [编辑]              │
│  ├─ 用户名: zhangsan                          │
│  ├─ 年龄: 28                                  │
│  ├─ 性别: 男                                  │
│  └─ 电话: 138****1234                        │
│                                                  │
│  饮食偏好设置                                   │
│  ├─ 健康目标: [减脂        ▼]                  │
│  ├─ 口味偏好: [清淡        ▼]                  │
│  ├─ 饮食限制: [无          ▼]                  │
│  └─ 烹饪水平: [进阶        ▼]                  │
│                                                  │
│           [保存设置]                            │
└─────────────────────────────────────────────────┘
```

图4-6 个人中心页面

**7. 我的收藏页面**

用户收藏的食谱会在收藏页面展示,方便日后查阅。实现效果如图4-7所示:

```
┌─────────────────────────────────────────────────┐
│  我的收藏                                         │
├─────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────┐ │
│  │ 番茄炒蛋                    [查看详情][取消收藏]│ │
│  │ ⏱ 15分钟  难度: 简单                        │ │
│  └─────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────┐ │
│  │ 土豆烧鸡腿                    [查看详情][取消收藏]│ │
│  │ ⏱ 30分钟  难度: 中等                        │ │
│  └─────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────┐ │
│  │ 清蒸鲈鱼                    [查看详情][取消收藏]│ │
│  │ ⏱ 25分钟  难度: 中等                        │ │
│  └─────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

图4-7 我的收藏页面

### 4.3 系统测试

#### 4.3.1 单元测试

系统采用单元测试验证各个功能模块的正确性。以下是主要功能模块的测试用例和结果:

**1. 用户登录功能测试**

| 编号 | 输入数据 | 预期结果 | 实际结果 | 是否通过 |
|------|---------|---------|---------|---------|
| 1-1 | 用户名:zhangsan 密码:123456 身份:用户 | 登录成功,进入主页面 | 登录成功 | 通过 |
| 1-2 | 用户名:zhangsan 密码:wrong 身份:用户 | 登录失败,提示密码错误 | 登录失败,提示密码错误 | 通过 |
| 1-3 | 用户名:nonexist 密码:123456 身份:用户 | 登录失败,提示无此账号 | 登录失败,提示无此账号 | 通过 |

**2. 食材管理功能测试**

| 编号 | 输入数据 | 预期结果 | 实际结果 | 是否通过 |
|------|---------|---------|---------|---------|
| 2-1 | 名称:番茄 数量:500g 类型:蔬菜 | 添加成功,返回食材ID | 添加成功 | 通过 |
| 2-2 | 上传番茄图片进行识别 | 返回识别结果:番茄,数量 | 返回番茄 | 通过 |
| 2-3 | 删除食材ID:5 | 删除成功 | 删除成功 | 通过 |
| 2-4 | 修改食材ID:5 数量:600g | 修改成功 | 修改成功 | 通过 |

**3. 食谱生成功能测试**

| 编号 | 输入数据 | 预期结果 | 实际结果 | 是否通过 |
|------|---------|---------|---------|---------|
| 3-1 | 食材:[番茄,鸡蛋] 数量:2 | 返回2个推荐食谱 | 返回2个食谱 | 通过 |
| 3-2 | 食材:[鸡肉,米饭] 饮食限制:无 | 返回适合的食谱 | 返回相应食谱 | 通过 |
| 3-3 | 食材:[空] | 提示选择至少一个食材 | 提示选择食材 | 通过 |

**4. 收藏功能测试**

| 编号 | 输入数据 | 预期结果 | 实际结果 | 是否通过 |
|------|---------|---------|---------|---------|
| 4-1 | 用户ID:1 食谱ID:3 | 收藏成功 | 收藏成功 | 通过 |
| 4-2 | 用户ID:1 食谱ID:3 | 提示已收藏 | 提示已收藏 | 通过 |
| 4-3 | 用户ID:1 食谱ID:3 | 取消收藏成功 | 取消成功 | 通过 |

**5. 营养分析功能测试**

| 编号 | 输入数据 | 预期结果 | 实际结果 | 是否通过 |
|------|---------|---------|---------|---------|
| 5-1 | 食谱ID:1 | 返回营养成分数据 | 返回热量、蛋白质等 | 通过 |
| 5-2 | 食谱ID:1 健康目标:减脂 | 返回评估和建议 | 返回建议 | 通过 |

**6. 图片生成功能测试**

| 编号 | 输入数据 | 预期结果 | 实际结果 | 是否通过 |
|------|---------|---------|---------|---------|
| 6-1 | 食谱ID:1 本地无图片 | 生成图片并保存 | 图片生成并保存 | 通过 |
| 6-2 | 食谱ID:1 本地已有图片 | 直接返回本地图片 | 返回本地图片 | 通过 |
| 6-3 | 图片访问:/picture/番茄炒蛋.jpg | 返回图片文件 | 返回图片 | 通过 |

#### 4.3.2 集成测试

集成测试验证多个模块协同工作的正确性:

**1. 食谱完整流程测试**

| 编号 | 测试步骤 | 预期结果 | 实际结果 | 是否通过 |
|------|---------|---------|---------|---------|
| 1-1 | 用户登录→添加食材→生成食谱→查看详情→收藏 | 全流程成功 | 全流程成功 | 通过 |
| 1-2 | 用户登录→设置偏好→生成食谱(考虑偏好) | 食谱符合偏好设置 | 生成符合偏好的食谱 | 通过 |

**2. 图片生成完整流程测试**

| 编号 | 测试步骤 | 预期结果 | 实际结果 | 是否通过 |
|------|---------|---------|---------|---------|
| 2-1 | 查看食谱详情→触发图片生成→前端显示 | 图片正常显示 | 图片显示正常 | 通过 |
| 2-2 | 查看已有食谱详情→直接使用本地图片 | 快速加载图片 | 图片秒开 | 通过 |

---

## 结论

本文设计并实现了一个基于大模型的个性化食谱生成系统,该系统利用人工智能技术为用户提供便捷、高效、个性化的饮食管理方案。

**主要完成的工作:**

1. 遵循软件工程过程的基本原理,对系统进行了完整的需求分析和系统设计。采用前后端分离的B/S架构,使用Vue.js 3和Spring Boot 3.2作为核心技术栈。

2. 设计并实现了用户管理、食材管理、食谱生成、收藏管理、营养分析等多个功能模块,满足了用户个性化食谱管理的核心需求。

3. 集成阿里云百炼平台的MiniMax-M2.7大语言模型,实现了智能食谱生成功能,能够综合考虑用户的口味偏好、饮食限制和健康目标。

4. 创新性地引入MiniMax image-01模型生成与食谱名称匹配的美食图片,并设计了本地缓存机制,提高了图片加载效率和用户体验。

5. 采用MySQL数据库进行数据持久化存储,设计了合理的数据库表结构,保证了数据的完整性和查询效率。

6. 对系统进行了全面的单元测试和集成测试,验证了系统功能的正确性和稳定性。

**系统特色:**

- **高度个性化**: 充分考虑用户的个体差异,生成符合个人需求的食谱方案
- **AI智能化**: 利用大语言模型的理解和生成能力,提升用户体验
- **图片匹配**: 通过AI生成匹配的美食图片,解决传统系统图片不匹配的问题
- **本地缓存**: 图片本地保存复用,提高加载效率,节省API调用成本

**不足与展望:**

系统还有一些可以改进的地方,未来的研究方向包括:

1. 引入更多数据源,完善食材营养数据库,提供更准确的营养分析
2. 增加食谱分享功能,支持用户之间的食谱分享和交流
3. 开发移动端应用,提供更便捷的移动使用体验
4. 引入机器学习算法,根据用户反馈不断优化推荐效果

---

## 致谢

四年的大学生活即将结束,于我而言,大学毕业是大学生活的一个句号,但在漫长的人生中,这仅仅只是一个逗号。我将面对新的生活和更加残酷的挑战。

本毕业设计及论文是在我的导师的亲切关怀和指导下完成的。在毕业设计的这段时间里,我遇到过很多难题,无论是学业上的还是生活上的,老师都会站在我的身后,我为能遇到这样一位治学严谨、学识渊博的老师感到骄傲。再次感谢老师的细心、耐心的指导和倾心的相助。同时也感谢大学四年中每一位老师,是他们用生动的课程引领我走入软件工程这个专业。也感谢一路上帮助我的同学们。

最后,祝我敬爱的老师们生活美满、万事如意;祝我亲爱的同学们前程似锦。

---

## 参考文献

[1] 向露. 基于SSM的智能停车场管理系统的设计与实现[J]. 电子设计工程, 2018, 26(13): 24-27.

[2] 刘琛, 倪雪莉, 张军. 基于物联网的城市无人值守停车场系统设计[J]. 物联网技术, 2019, 9(1): 66-68.

[3] 李奕富. 基于远程服务系统的停车场出入口无人值守管理系统设计[J]. 数字化用户, 2017, 17: 65.

[4] Lin Lin. Low power consumption and reliability of wireless communication network in intelligent parking system[J]. Journal of Ambient Intelligence and Humanized Computing, 2021, 12: 1305-1313.

[5] Craig Walls. Spring in Action[M]. Manning: American, 2014: 15-40.

[6] 牛德雄. Java EE(SSH框架)软件项目开发案例教程[M]. 北京: 电子工业出版社, 2016: 17-36.

[7] 李林锋. 分布式服务框架原理与实践[M]. 北京: 电子工业出版社, 2016: 76-93.

[8] 疯狂软件. Spring+Mybatis企业应用实战[M]. 北京: 电子工业出版社, 2017: 20-50.

[9] 刘彦君, 金飞虎. JavaEE开发技术与案例教程[M]. 北京: 人民邮电出版社, 2014: 50-74.

[10] 徐洪禹. 面向只读应用的分布式数据库的模型构造及查询优化的研究[D]. 大连理工大学, 2000: 21-33.

[11] 丁轼轩. 基于Java的停车管理系统的设计与实现[J]. 电子技术与软件工程, 2018, 5: 149-150.

[12] Kazuhiro Ogata. A Generic Approach on How to Formally Specify and Model Check Path Finding Algorithms[J]. World Scientific Publishing, 2020, 30(10): 1481-1523.

[13] 王祥顺. 软件工程的安全检测和维护[J]. 电子技术与软件工程, 2017, 9: 36.

[14] Cao Jing, Liu Xiaoqiang, Guo Hui, et al. Test Case Generation for Web Application Based on Markov Reward Process[J]. Journal of Physics, 2020, 1792: 18-20.

[15] Sun Tingting, Lv Xingjun, Cai Yakun, et al. Software test quality evaluation based on fuzzy mathematics[J]. Journal of Intelligent & Fuzzy Systems, 2021, 40(4): 6125-6135.

---

**文档说明**: 本文档根据哈尔滨理工大学软件工程专业本科毕业设计模板编写,基于"基于大模型的个性化食谱生成系统"项目内容填充完整。可根据实际情况调整部分内容,如导师信息、具体数值等。
